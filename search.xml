<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>2024物理考研调剂</title>
    <url>/2024/03/26/2024%E7%89%A9%E7%90%86%E8%80%83%E7%A0%94%E8%B0%83%E5%89%82/</url>
    <content><![CDATA[<h1 id="北京工业大学-调剂信息"><a href="#北京工业大学-调剂信息" class="headerlink" title="北京工业大学  调剂信息"></a>北京工业大学  <a href="https://yanzhao.bjut.edu.cn/info/1019/15739.htm">调剂信息</a></h1><blockquote>
<h3 id="还没开始进行复试，调剂名额为确定"><a href="#还没开始进行复试，调剂名额为确定" class="headerlink" title="还没开始进行复试，调剂名额为确定"></a>还没开始进行复试，调剂名额为确定</h3></blockquote>
<h3 id="录取人数信息："><a href="#录取人数信息：" class="headerlink" title="录取人数信息："></a>录取人数信息：</h3><img src="/2024/03/26/2024%E7%89%A9%E7%90%86%E8%80%83%E7%A0%94%E8%B0%83%E5%89%82/image-20240326144820388.png" class="" title="image-20240326144820388">

<h3 id="接受调剂的学院及调剂系统开放时间"><a href="#接受调剂的学院及调剂系统开放时间" class="headerlink" title="接受调剂的学院及调剂系统开放时间"></a>接受调剂的学院及调剂系统开放时间</h3><img src="/2024/03/26/2024%E7%89%A9%E7%90%86%E8%80%83%E7%A0%94%E8%B0%83%E5%89%82/image-20240326145030194.png" class="" title="image-20240326145030194">

<h3 id="调剂的要求"><a href="#调剂的要求" class="headerlink" title="调剂的要求"></a>调剂的要求</h3><blockquote>
<p>（1）符合申请调入学科专业的报考条件。</p>
<p>（2）符合申请调入学科专业的调剂基本要求（含考生第一志愿专业范围、 初试科目、初试成绩要求等）。 （3）初试成绩应符合第一志愿报考学科专业国家A 类考生分数线并且达到调入学科专业复试分数线要求。 （4）初试科目与调入学科专业初试科目相同或相近，其中初试全国统一命题科目应与调入学科专业全国统一命题科目相同。</p>
<p>（5）根据我校人才选拔要求，初试科目为英语（二）的考生，不能调剂到初试科目为英语（一）的学科专业；初试科目为数学（二）的考生，不能调剂到初试科目为数学（一）的学科专业；初试科目为数学（三）的考生，不能调剂到初试科目为数学（一）或数学（二）的学科专业；初试科目为314数学（农）或 396 经济类综合能力的考生，不能调剂到初试科目为数学（一）、数学（二）或数学（三）的学科专业。</p>
</blockquote>
<img src="/2024/03/26/2024%E7%89%A9%E7%90%86%E8%80%83%E7%A0%94%E8%B0%83%E5%89%82/image-20240326145153124.png" class="" title="image-20240326145153124">



<h3 id="今年的复试分数线"><a href="#今年的复试分数线" class="headerlink" title="今年的复试分数线"></a>今年的复试分数线</h3><table>
<thead>
<tr>
<th>材料科学 070200</th>
<th>物理学</th>
<th>学术学位</th>
<th>41</th>
<th>41</th>
<th>62</th>
<th>62</th>
<th>288</th>
</tr>
</thead>
<tbody><tr>
<td><strong>理学部 070200</strong></td>
<td><strong>物理学</strong></td>
<td><strong>学术学位</strong></td>
<td><strong>41</strong></td>
<td><strong>41</strong></td>
<td><strong>62</strong></td>
<td><strong>62</strong></td>
<td><strong>288</strong></td>
</tr>
</tbody></table>
<p>去年接受调剂同学的信息</p>
<img src="/2024/03/26/2024%E7%89%A9%E7%90%86%E8%80%83%E7%A0%94%E8%B0%83%E5%89%82/image-20240326145514150.png" class="" title="image-20240326145514150">





<h1 id="杭州电子科技大学211"><a href="#杭州电子科技大学211" class="headerlink" title="杭州电子科技大学211"></a>杭州电子科技大学211</h1><p>这个学校可以看一下，他去年的理学院的物理学招了大量的调剂37个，今年是拟录取31<a href="https://grs.hdu.edu.cn/2023/0921/c1721a250938/page.htm">录取信息</a></p>
<p>没找到今年的复试线，不过感觉过线没问题</p>
<p>今年的复试时间没看到，不过学校的复试时间是3月29日- 4月20日进行。 <a href="https://grs.hdu.edu.cn/2024/0325/c1721a262471/page.htm">具体复试安排</a></p>
<p>去年调剂的信息：</p>
<img src="/2024/03/26/2024%E7%89%A9%E7%90%86%E8%80%83%E7%A0%94%E8%B0%83%E5%89%82/image-20240326154212334.png" class="" title="image-20240326154212334">





<p>1111</p>
]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2023/09/17/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<span id="more"></span>

<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ hexo new <span class="token string">"My New Post"</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ hexo server<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ hexo generate<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">$ hexo deploy<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>golang</title>
    <url>/2024/02/26/golang/</url>
    <content><![CDATA[<h1 id="学习过程中的一些小知识点"><a href="#学习过程中的一些小知识点" class="headerlink" title="学习过程中的一些小知识点"></a>学习过程中的一些小知识点</h1><ul>
<li><p>Goroutine之间的通信并不是共享一段内存，在这段内存中来保存数据设置锁。而是通过在进程之间通信来实现共享</p>
<blockquote>
<p>Don’t communicate by sharing memory, share memory by communicating</p>
</blockquote>
</li>
<li><p>在Google中，搜索一个关键词时，会搜索所有相关的内容（图片、视频等）。而且对于每个类型都会进行多次的搜索，而对于相同类型的内容，我们只采取第一次的数据。<img src="/2024/02/26/golang/image-20240302210841089.png" class="" title="image-20240302210841089">从这里我们就可以看出来，我们在通道c收到了一次数据之后就直接返回了。</p>
</li>
<li><p>在每一个源文件中，全局变量会先被定义，然后是执行init（）函数最后是执行main（），所以我们可以在init函数中完成初始化的工作。&#x3D;&#x3D;注意，如果是别的文件使用我们当前文件中的变量时，在导入包的过程中就会执行当前包中的init()函数，来初始化需要引入的变量。然后如果导入的文件中也有变量的定义和初始化的话，时在需要导入的完成之后在进行处理。&#x3D;&#x3D;</p>
</li>
<li><p>&#x3D;&#x3D;匿名函数&#x3D;&#x3D;</p>
<pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">package</span> main

<span class="token keyword">import</span> <span class="token string">"fmt"</span>

<span class="token keyword">func</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
	ans <span class="token operator">:=</span> <span class="token keyword">func</span><span class="token punctuation">(</span>x<span class="token punctuation">,</span> y <span class="token builtin">int</span><span class="token punctuation">)</span> <span class="token builtin">int</span> <span class="token punctuation">&#123;</span>
		<span class="token keyword">return</span> x <span class="token operator">+</span> y
	<span class="token punctuation">&#125;</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span>

	sub <span class="token operator">:=</span> <span class="token keyword">func</span><span class="token punctuation">(</span>x<span class="token punctuation">,</span> y <span class="token builtin">int</span><span class="token punctuation">)</span> <span class="token builtin">int</span> <span class="token punctuation">&#123;</span>
		<span class="token keyword">return</span> x <span class="token operator">-</span> y
	<span class="token punctuation">&#125;</span>
	ans2 <span class="token operator">:=</span> <span class="token function">sub</span><span class="token punctuation">(</span><span class="token number">20</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">)</span>
	
	fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>ans<span class="token punctuation">,</span> ans2<span class="token punctuation">)</span>
<span class="token punctuation">&#125;</span>
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>从这里我们可以看到，我们可以直接定义一个没有具体名称的函数，并且在函数定义的后面加上小括号进行调用。或者我们可以讲一个匿名函数绑定一个名称，通过这个名称去进行调用。而全局匿名函数的使用就是在文件中直接定义函数，并将函数给到一个变量。&#x3D;&#x3D;不过用的最多的方法就是直接在函数中进行匿名函数的定义和使用&#x3D;&#x3D;</p>
</li>
<li><p>闭包的相关内容：在我的理解中，闭包就是一个返回函数类型的函数，但是返回的这个函数可以引用返回她的函数中的变量。&#x3D;&#x3D;我的理解：我感觉就像是一个服务器在接受到请求之后，现在闭包中做一下响应前的一些操作，然后再去进行响应。&#x3D;&#x3D;</p>
</li>
<li><p>工厂模式就是，我在一个包中不直接暴露我的结构体，而是通过一个函数来返回一个结构体的指针。这样外部的文件就无法直接的操作我包中定义的结构体了</p>
</li>
<li><p>go中的封装、继承和多态</p>
<blockquote>
<p>在go中，&#x3D;&#x3D;因为go又一个大写字母会从当前文件进行暴露的特性，所以我们可以根据这个特性来进行设计（其实就是通过包来实现封装）&#x3D;&#x3D;。我们可以创建一个结构体，让我们的属性变量设置小写的字母，这样我们在暴露出结构体的时候，外部用的用户无法直接的操作我们的隐藏的变量。然后我们可以通过方法来获取一个结构体内部的相关属性。</p>
</blockquote>
</li>
<li><p>Go 的继承是通过在一个结构体中使用另外一个结构体来进行实现的。</p>
<blockquote>
<p>不过需要注意的是，如果这两个结构体的定义是在一个包中的话，那么引用别人的结构体是能够直接访问到被引用的结构体的，但是如果两者不在一个包中，那么只能通过GET函数来进行获取</p>
</blockquote>
</li>
<li><p>Defer 、panic和recover</p>
<blockquote>
<p>注意defer 有一下三个特点</p>
<ol>
<li><p>defer语句中的参数在运行到defer的语句所在行时进行计算</p>
<pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token function">a</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    i <span class="token operator">:=</span> <span class="token number">0</span>
    <span class="token keyword">defer</span> fmt<span class="token punctuation">.</span><span class="token function">Println</span><span class="token punctuation">(</span>i<span class="token punctuation">)</span>
    i<span class="token operator">++</span>
    <span class="token keyword">return</span>
<span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>例如上面这个案例，最终将会输出0</p>
</li>
<li><p>延迟函数是一个后进先出的执行过程，例子如下</p>
<pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token function">b</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">for</span> i <span class="token operator">:=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> <span class="token number">4</span><span class="token punctuation">;</span> i<span class="token operator">++</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">defer</span> fmt<span class="token punctuation">.</span><span class="token function">Print</span><span class="token punctuation">(</span>i<span class="token punctuation">)</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>我感觉因为go的defer是使用stack来保存相关的函数的，所以最后压入栈的会先被执行</p>
</li>
<li><p>延迟函数可以修改返回值</p>
<pre class="line-numbers language-go" data-language="go"><code class="language-go"><span class="token keyword">func</span> <span class="token function">c</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">(</span>i <span class="token builtin">int</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">defer</span> <span class="token keyword">func</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span> i<span class="token operator">++</span> <span class="token punctuation">&#125;</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
    <span class="token keyword">return</span> <span class="token number">1</span>
<span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre>

<p>我们从这个例子中，可以看到c()函数会先返回一个1，然后延迟函数一执行就会把i修改为2。所以我感觉defer 语句如果是一个函数的话，那这个函数就是一个闭包，他会持续的引用原函数中的变量，直至闭包结束。</p>
</li>
</ol>
<p>Panic：恐慌可以由程序员执行调用，也可以在程序发生运行时错误时出发。恐慌发生时，正常的空自流会中止，但是defer函数会正常的继续执行。然后等到所有的defer语句执行完毕后，如果恐慌没有被恢复，那么会继续向函数的调用者传递恐慌。</p>
<p>Recover：会中止函数的恐慌，然后正常的执行控制流的内容。</p>
</blockquote>
</li>
</ul>
]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
  <entry>
    <title>k8s</title>
    <url>/2024/01/31/k8s/</url>
    <content><![CDATA[<h1 id="Docker内容"><a href="#Docker内容" class="headerlink" title="Docker内容"></a>Docker内容</h1><h2 id="Docker容器底层隔离"><a href="#Docker容器底层隔离" class="headerlink" title="Docker容器底层隔离"></a>Docker容器底层隔离</h2><p>​	docker容器的底层隔离主要通过Linux的namespace和cgroup来进行的，其中通过namespace来进行资源的隔离，使不同的资源只能够看到需要的内容。通过cgroup来约束进程对于资源的使用。详细的可以查看<a href="https://www.cnblogs.com/DarkH/p/16366020.html%E3%80%82">https://www.cnblogs.com/DarkH/p/16366020.html。</a></p>
<h2 id="Docker的一些知识点"><a href="#Docker的一些知识点" class="headerlink" title="Docker的一些知识点"></a>Docker的一些知识点</h2><ol>
<li><p>在pull时关于版本的问题，之前学习docker的时候没有关注这一块的内容。不过这里需要注意，因为一个容器是包含这个软件本身和它运行所需要的环境的。而且一般的运行环境都需要linux，这时如果我们随意的选择Linux版本的话，就可能导致相同功能的两个镜像，大小（size）的差别是很大的。&#x3D;&#x3D;我们一般推荐在pull镜像的时候，优先选择精简版本的运行环境，例如：alpine，slim&#x3D;&#x3D;</p>
</li>
<li><p>注意docker create 命令只是创建一个容器，但是这个容器并不是启动的。</p>
</li>
<li><p>再写端口映射的时候，xxx：xxx，前面的是宿主机端口，外网可以直接访问。后面的是容器内部的端口</p>
</li>
<li><p>注意一个容器可以绑定多个宿主机的端口（即写多个-p）</p>
</li>
<li><p>docker run默认是前台启动，所以我们需要手动的添加-d来让容器后台启动（其实docker run -d&#x3D;&#x3D;docker create+docker start）</p>
</li>
<li><p>注意之前没有特别的学习过docker logs，主要用于查看后台启动的容器的日志。</p>
</li>
<li><p>注意我们如果需要使用docker attach这个命令的时候，attach是将当前的一个控制台绑定到容器，当我们控制台与容器断开连接时（即按了ctrl+c），就可能会导致容器停止。</p>
</li>
<li><p>所以我们一般使用docker exec这个命令来与容器进行交互。在这个命令中我们可以加上本次操作的用户名以及用户组，以及可以加上操作的权限。可以让我们最大程度的对容器进行操作。</p>
</li>
<li><p>我们在需要修改容器内部文件的时候，可以现在外面配置好，然后使用docker cp的命令来进行文件复制的操作。这样就有利于我们操作的便捷性。</p>
<blockquote>
<p>但是我们需要注意的是，src和dest分别为不同值时，cp命令的效果</p>
<ul>
<li>当src是一个指定文件时<ul>
<li>dest如果不存在，则会先创建一个dest的文件，并且dest的内容为src的内容</li>
<li>dest不存在且以&#x2F;结束，报错</li>
<li>dest存在且是一个文件，则dest的内容替换为src的内容</li>
<li>dest存在且是一个目录，src会复制到dest目录下面</li>
</ul>
</li>
<li>当src是一个目录时<ul>
<li>dest不存在，则创建一个名为dest的文件夹，并复制src中的内容</li>
<li>dest是一个文件时，报错</li>
<li>dest是一个目录<ul>
<li>如果src路径不是以&#x2F;.结尾，那么src整个目录会被复制到dest下面</li>
<li>如果src路径是以&#x2F;.结尾的，那么src中的内容会被复制到dest下面</li>
</ul>
</li>
</ul>
</li>
</ul>
</blockquote>
</li>
<li><p>我们可以使用docker diff来查看当前的容器内部的内容与之前有什么不同，之后我们可以使用docker commit来提交我们当前的最新版本。具体的指令使用可以参考docker commit –help。</p>
</li>
<li><p>&#x3D;&#x3D;需要注意一下，我们之前提到的游离镜像在commit时会出现&#x3D;&#x3D;，当我们同名的镜像在commit的时候(并且版本号也相同的时候)，之前的镜像的名字和版本都会变成none（即游离镜像），这个时候我们在使用docker image prune即可删除游离镜像。</p>
</li>
<li><p>&#x3D;&#x3D;重点：推送镜像&#x3D;&#x3D;</p>
<blockquote>
<ol>
<li>注册docker hub并登陆。</li>
<li>创建一个仓库</li>
<li>修改本地的容器名为docker hub的用户名&#x2F;容器名（因为在dockerhub上，每一个镜像都有一个完整的路径，其中官方的路径是docker.io&#x2F;library&#x2F;xxx，而用户自己创建的镜像的完整路径是：docker.io&#x2F;用户名&#x2F;镜像名:tag。所以，我们在推送自己的镜像之前需要先修改镜像的名称）</li>
<li>我们在推送之前先需要在控制台上登陆我们的docker，否则无法判断推送到哪个用户下。使用docker login进行登陆。可以看.docker下面的config.json来查看是否登陆成功。</li>
<li>使用docker push 用户名&#x2F;镜像名：tag进行推送</li>
</ol>
</blockquote>
</li>
<li><p>当我们使用u盘等工具进行容器和镜像的传输时，有相应的命令可以使用</p>
<blockquote>
<p>docker export和docker import</p>
<p>这两个命令是操作容器时使用的，我们可以使用docker export将一个容器中的内容到处为一个tar文件，然后我们可以使用docker import将这个tar文件创建为一个镜像。&#x3D;&#x3D;但是这个镜像是无法正常启动的，他需要之前容器启动时的命令，我们可以通过docker ps –no-trunc来查看原来的容器的命令&#x3D;&#x3D;。</p>
<p>docker save和docker load</p>
<p>这两个命令是操作镜像用的，我们可以使用docker save直接讲一个镜像设置成一个tar文件，然后使用docker load将这个tar文件直接加载为一个镜像。<del>&#x3D;&#x3D;注意这个也是需要用之前的命令来启动的&#x3D;&#x3D;</del></p>
</blockquote>
</li>
<li><p>Docker 容器长久运行的原理。</p>
<blockquote>
<p>我们知道docker容器中跑了一个软件，这个软件会有一个阻塞的进程在一直运行，所以才能够让我们的容器一直运行。如果我们容器中的软件没有一个阻塞的进程的话，容器会启动，完成相应的命令之后就会关闭容器。例如：busybox默认会运行一个sh，在这个sh运行完成之后就会关闭容器。</p>
</blockquote>
</li>
<li><p>我们可以使用docker events来查看docker的事件（包含了几乎所有的内容）</p>
</li>
</ol>
<h2 id="Docker中的一些底层原理"><a href="#Docker中的一些底层原理" class="headerlink" title="Docker中的一些底层原理"></a>Docker中的一些底层原理</h2><h3 id="Docker存储"><a href="#Docker存储" class="headerlink" title="Docker存储"></a>Docker存储</h3><p>​	docker的镜像和容器的存储是分层，他可以是根据dockerfile的每一句话来生成一层。因为分层之后我们就可以只需要修改当前层的内容即可，底层的内容可以共同进行使用降低存储的成本。</p>
<h3 id="容器与镜像的写时复制机制"><a href="#容器与镜像的写时复制机制" class="headerlink" title="容器与镜像的写时复制机制"></a>容器与镜像的写时复制机制</h3><p>​	我们的容器是在镜像的基础上运行的，一个镜像启动的容器会共同使用这个镜像的底层层次。如果一个容器需要读某个文件的话，直接读取镜像中对应的文件即可。但是如果容器需要对镜像中的某个文件进行修改的话，例如：nginx的配置文件需要修改，这是我们需要在容器中新创建一个文件对应镜像中的文件，当容器需要修改的时候，直接修改自己新建的文件即可。从此之后容器再次访问这个文件时，会直接访问当前容器上层中的文件，而不是镜像中的文件。（&#x3D;&#x3D;需要注意的是，如果我们要查看的话，需要在宿主机中查看，而不是进入到容器内部来查看。而且无论我们如何修改，底层的镜像是不会改变的&#x3D;&#x3D;）<del>但是自己并没有实验出来，在外面查看的时候发现进不去&#x2F;etc&#x2F;nginx（这里以nginx为例）</del>(最终经过实验后成功了！！！)</p>
<h3 id="Docker如果保存容器中的数据"><a href="#Docker如果保存容器中的数据" class="headerlink" title="Docker如果保存容器中的数据"></a>Docker如果保存容器中的数据</h3><p>容器挂载：</p>
<p>​	每一个容器里面的内容，支持三种挂载的方式：</p>
<blockquote>
<ol>
<li>docker自动在外部创建文件夹并与容器内部的文件夹相关联（Dockerfile VOLUME指令的作用）</li>
<li>自己手动在外部创建文件夹，并且手动的进行挂载（使用Bind mounts）</li>
<li>将数据挂载到内存中（tmpfs mounts）</li>
</ol>
</blockquote>
<p>​	</p>
<p>​	docker使用 <code>-v</code>进行挂载有一下几种情况（以nginx为例）:</p>
<blockquote>
<ol>
<li>不挂载：默认访问欢迎页</li>
<li>使用以&#x2F;开头的绝对路径进行挂载：会访问绝对路径下面下的index.html</li>
<li>使用不已&#x2F;开头的路径进行挂载：会访问默认欢迎页面</li>
</ol>
</blockquote>
<p>​	下面来解释一下具体的原因，当我们不进行挂载的时候，容器中的数据会默认保存在容器中。当我们的容器删除的时候，容器内的数据也会被相应的删除掉。所以我们必须要进行数据的迁移和挂载。</p>
<p>​	当我们使用以&#x2F;开头的绝对路径进行挂载时，容器会将对应路径下面的内容替换为我们设置的绝对路径，注意&#x3D;&#x3D;这个时候很容器出现空挂载的问题（即容器的配置文件在宿主机中的对应路径下面是一个空的文件夹这样就会导致容器启动失败）&#x3D;&#x3D;</p>
<p>​	当我们使用不以&#x2F;开头的路径进行挂载时，其实就是让docker来帮我们来创建一个以给出的名字命名的卷来存储我们需要启动的容器的数据。&#x3D;&#x3D;而且，这个卷会复制容器初始的config和一些页面之类的内容&#x3D;&#x3D;，所以我们用这种方式打开的时候能够看到默认的访问页面。(&#x3D;&#x3D;注意这里删除了容器之后也不会删除这个卷，其实也是正常的，如果删除了这个卷，那我们创建创建这个卷有什么用呢&#x3D;&#x3D;)</p>
<h2 id="Dockerfile"><a href="#Dockerfile" class="headerlink" title="Dockerfile"></a>Dockerfile</h2><h4 id="FROM指令"><a href="#FROM指令" class="headerlink" title="FROM指令"></a>FROM指令</h4><p>​	from指令是用来为我们镜像确定一个基础的环境的语句，而且一个dockerfile中只能够有一个from指令。</p>
<h4 id="ARG指令"><a href="#ARG指令" class="headerlink" title="ARG指令"></a>ARG指令</h4><p>​	arg指令是用来设置一些&#x3D;&#x3D;构建阶段&#x3D;&#x3D;中使用的临时变量而使用的指令，只有在arg指令设置了之后才能够在之后的构建过程中进行使用。而且我们也可以通过在docker build的时候使用 –build-arg选项来修改我们的arg指令设置的参数。可以在RUN指令进行使用。</p>
<h4 id="ENV指令"><a href="#ENV指令" class="headerlink" title="ENV指令"></a>ENV指令</h4><p>​	env指令设置的变量可以在&#x3D;&#x3D;构建和运行阶段&#x3D;&#x3D;使用，但是指令的&#x3D;&#x3D;修改只能够在运行时&#x3D;&#x3D;通过添加–env来进行修改。</p>
<h4 id="ADD和COPY命令"><a href="#ADD和COPY命令" class="headerlink" title="ADD和COPY命令"></a>ADD和COPY命令</h4><p>​	这两个命令都是从宿主机中选择相应的文件复制到容器中，其中ADD可以从远程remote拉取文件或者从本地复制文件，而copy只能够从本地获取。&#x3D;&#x3D;需要注意的是，我们在写src的时候，一定要让src在镜像构建时可以访问的上下文范围内，否则将会构建失败。&#x3D;&#x3D;</p>
<p>​	这两个命令都可以在复制文件时，添加文件访问的相关权限。&#x3D;&#x3D;但是需要注意这里添加的用户名和用户组必须是容器或者镜像环境中存在的才可以，否则构建时报错。&#x3D;&#x3D;</p>
<p>​	需要注意一点，如果我们的镜像的基础环境中，没有&#x2F;etc&#x2F;passwd和&#x2F;etc&#x2F;group，并且我们在使用这两个命令时使用用户名和用户组添加相关的权限会出现无法匹配，从而导致报错。但是，如果使用的是数字来设置相关的访问权限的话，就不会导致报错。</p>
<h4 id="WORKDIR指令"><a href="#WORKDIR指令" class="headerlink" title="WORKDIR指令"></a>WORKDIR指令</h4><p>​	我们可以通过这个指令来设置我们之后的一些指令工作的目录。如果不设置这个选项的话，默认情况下为&#x2F;或者由你的基础环境设置。同时workdir支持相对路径的设置（即可以设置多个，他自己默认讲这些路径结合起来）</p>
<h4 id="CMD和ENTRYPOINT指令"><a href="#CMD和ENTRYPOINT指令" class="headerlink" title="CMD和ENTRYPOINT指令"></a>CMD和ENTRYPOINT指令</h4><p>​	这两个指令都是用于容器启动时的命令，这两个单独作用都是可以的。而且这两个命令都有两种写法（即exec写法和shell写法，其中需要注意的是：&#x3D;&#x3D;直接使用exec是无法读到ENV设置的参数的，不过我们可以让指令为&#x2F;bin&#x2F;sh -c，然后再接上参数就可以读取到相应的参数了&#x3D;&#x3D;）</p>
<p>​	其中CMD的优先级是低于ENTRYPOINT的。当这两个都写的时候，&#x3D;&#x3D;运行的指令为ENTRYPOINT+CMD的内容，且二者合起来的命令是一个正确完整的命令&#x3D;&#x3D;。</p>
<p>​	我们可以通过在启动容器时添加相应的参数来修改掉CMD的内容，但是ENTRYPOINT是一定运行的。</p>
<p>​	当dockerfile中出现多个CMD和ENTRYPOINT的时候，只有最后一个会生效。</p>
<h2 id="Docker网络"><a href="#Docker网络" class="headerlink" title="Docker网络"></a>Docker网络</h2><p>​	在我们的应用中，有时需要两个容器进行通信或者容器可以与外界进行交互。这个时候就会产生一些问题。而我们的docker会先创建一个名为docker0的网络，我们在没有自定义网络的情况下，都会将运行的容器加入到这个网络中。</p>
<p>​	桥接模式可以看下面这个博客的文章，讲的比较详细：<a href="https://www.cnblogs.com/hahaha111122222/p/13370773.html%E3%80%82%E5%85%B6%E4%B8%AD%EF%BC%8C%E5%A6%82%E6%9E%9C%E6%88%91%E4%BB%AC%E4%BD%BF%E7%94%A8%E6%A1%A5%E6%8E%A5%E6%A8%A1%E5%BC%8F%E7%9A%84%E8%AF%9D%EF%BC%8C%E4%B8%8D%E8%83%BD%E5%A4%9F%E9%80%9A%E8%BF%87%E5%9F%9F%E5%90%8D%E6%9D%A5%E8%BF%9B%E8%A1%8C%E4%BA%92%E9%80%9A%EF%BC%8C%E5%8F%AA%E8%83%BD%E9%80%9A%E8%BF%87ip%E6%9D%A5%E8%BF%9B%E8%A1%8C%E9%80%9A%E4%BF%A1%E3%80%82%E4%BD%86%E6%98%AF%EF%BC%8C%E8%BF%99%E6%A0%B7%E5%B0%B1%E4%BC%9A%E6%9C%89%E5%B1%80%E9%99%90%E6%80%A7%EF%BC%8C%E5%9B%A0%E4%B8%BA%E6%88%91%E4%BB%AC%E6%AF%8F%E6%AC%A1%E5%90%AF%E5%8A%A8%E5%AE%B9%E5%99%A8%E6%97%B6%EF%BC%8Cdhcp%E7%BB%99%E5%88%86%E9%85%8D%E7%9A%84ip%E5%8F%AF%E8%83%BD%E4%BC%9A%E4%B8%8D%E5%90%8C%EF%BC%8C%E8%BF%99%E6%A0%B7%E5%B0%B1%E5%8F%AF%E8%83%BD%E5%AF%BC%E8%87%B4%E8%AE%BF%E9%97%AE%E7%9A%84ip%E5%87%BA%E9%94%99%E3%80%82">https://www.cnblogs.com/hahaha111122222/p/13370773.html。其中，如果我们使用桥接模式的话，不能够通过域名来进行互通，只能通过ip来进行通信。但是，这样就会有局限性，因为我们每次启动容器时，dhcp给分配的ip可能会不同，这样就可能导致访问的ip出错。</a></p>
<p>​	下面我们说一下自定义网络，我们在自定义网络时，可以添加一些子网掩码、网关之类的信息。我们之前使用网络想要实现几个容器之间的通信需要使用–link（其实就是修改&#x2F;etc&#x2F;host 来增加一个解析项来使得能够解析到）。但是这种方式会出现一种问题，就是容器重新启动的时候ip可能会发生改变，从而导致这种设置失效。但是如果我们自己创建一个网络的话，每当启动一个容器的时候就将这个容器加入到这个网络中，我们的网络为这个容器分配一个ip。每次重启每次重新分配，这时通过自定义网络的记录，就可以准确的找到需要转发的目的地。&#x3D;&#x3D;如果我们需要两个网络中的容器进行通信的话，可以使用“docker network connnet 网络名 容器名”的方式来将我们这个容器加入到指定的网络中&#x3D;&#x3D;</p>
<h3 id="Docker-compose内容"><a href="#Docker-compose内容" class="headerlink" title="Docker compose内容"></a>Docker compose内容</h3><p>​	这个部分的内容，自己根据docker的官网手册来做一遍就可以，基本的都是之前学过的东西，只不过写在一个yaml文件中而已。</p>
<h1 id="CICD内容"><a href="#CICD内容" class="headerlink" title="CICD内容"></a>CICD内容</h1><p>​	持续集成、持续部署</p>
<h1 id="Jenkins内容"><a href="#Jenkins内容" class="headerlink" title="Jenkins内容"></a>Jenkins内容</h1><p>​	注意docker pipeline是我们在Jenkinsfile中需要镜像时，直接使用docker的镜像时所需要的工具。</p>
<h2 id="Jenkins流水线使用docker启动sidecar容器"><a href="#Jenkins流水线使用docker启动sidecar容器" class="headerlink" title="Jenkins流水线使用docker启动sidecar容器"></a>Jenkins流水线使用docker启动sidecar容器</h2><img src="/2024/01/31/k8s/%E6%88%AA%E5%B1%8F2024-02-22%2000.58.02.png" class="">

<p>​	<del>这个地方再去看看docker  pipeline之后再做结论，感觉是docker pipeline实现的这个功能。</del>应该是Jenkins在运行docker容器的时候，自动把Jenkins的家目录挂载到了容器上面，不过我没有找到具体是如何执行这个容器的。</p>
<h1 id="K8S内容"><a href="#K8S内容" class="headerlink" title="K8S内容"></a>K8S内容</h1>]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
  <entry>
    <title>vue+ts学习</title>
    <url>/2024/01/23/vue-ts%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h1 id="选项式和组合式的区别"><a href="#选项式和组合式的区别" class="headerlink" title="选项式和组合式的区别"></a>选项式和组合式的区别</h1><p>选项式：vue2的样式，将数据data、方法methods之类的单独进行书写。</p>
<p>组合式：vue3的样式，数据、方法等内容都写在setup中。</p>
<p>&#x3D;&#x3D;注意，在setup中无法使用this，而在vue2中是可以使用setup的。&#x3D;&#x3D;</p>
<p>并且vue2和vue3可以混写，并且能够在data、methods通过this访问到setup中的内容。但是setup是访问不到data、methods中的内容的。</p>
<h2 id="setup的语法糖"><a href="#setup的语法糖" class="headerlink" title="setup的语法糖"></a>setup的语法糖</h2><p>比较正式的写法：</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">&lt;script lang&#x3D;&quot;ts&quot;&gt;
  export default&#123;
    name:&quot;Person&quot;,
    setup()&#123;
      ......
    &#125;
  &#125;
&lt;&#x2F;script&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>在这个地方中，我们需要使用name来确定当前组件的名称，并且在setup定义相关的内容。</p>
<p>在使用语法糖之后就会变得很简单，例如下面展示：</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">&lt;script lang&#x3D;&quot;ts&quot;&gt;
  export default&#123;
    name:&quot;Person&quot;
  &#125;
&lt;&#x2F;script&gt;
&lt;script lang&#x3D;&quot;ts&quot; setup&gt;
  ......
&lt;&#x2F;script&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>但是我们可以看到这里使用了两个script来完成这个操作，也不是特别的方便。所以，我们可以加入一个插件来使得script标签中可以直接加入一个name的属性来设置我们组件的名称。</p>
<p>我们需要添加vite-plugin-vue-setup-extend，然后在vite.config.ts中引用，并且在plugins中进行添加。<img src="/2024/01/23/vue-ts%E5%AD%A6%E4%B9%A0/%E6%88%AA%E5%B1%8F2024-01-23%2019.40.33.png" class=""></p>
<p>添加了这个插件之后就可以在我们的文件中可以直接使用name属性。</p>
<h1 id="响应式数据"><a href="#响应式数据" class="headerlink" title="响应式数据"></a>响应式数据</h1><h2 id="Ref方法"><a href="#Ref方法" class="headerlink" title="Ref方法"></a>Ref方法</h2><p>我们如果需要某个变量是响应式的，就需要使用ref将这个变量包裹起来，并且在template中使用的时候直接使用即可，但是在setup中使用时，需要使用name.value的格式，例如下面：</p>
<pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> name <span class="token operator">=</span> <span class="token function">ref</span><span class="token punctuation">(</span><span class="token string">"张三"</span><span class="token punctuation">)</span>

<span class="token keyword">function</span> <span class="token function">handleName</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">&#123;</span>
  name<span class="token punctuation">.</span>value <span class="token operator">=</span> <span class="token string">"李四"</span>
<span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>此时如果某个操作调用了handleName的方法的话就会使得name变成李四，并且立即在页面上进行响应。</p>
<h1 id="watch监视数据内容"><a href="#watch监视数据内容" class="headerlink" title="watch监视数据内容"></a>watch监视数据内容</h1><p>watch一共可以监视4中类型的数据</p>
<blockquote>
<ol>
<li>ref定义的数据</li>
<li>reactive定义的数据</li>
<li>函数返回一个值（getter函数）</li>
<li>一个包含上述内容的数组</li>
</ol>
</blockquote>
<p>但是我们总共能够碰到以下5种情况：</p>
<ol>
<li><p>情况一：监视ref定义的基本类型数据，这个很简单。下面给一个具体的样例：</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">&lt;script setup name&#x3D;&quot;Person&quot;&gt;
import &#123;ref, watch&#125; from &quot;vue&quot;;

let sum &#x3D; ref(0)

function addSum() &#123;
  sum.value++;
&#125;

let stop &#x3D; watch(sum, (value, oldValue) &#x3D;&gt; &#123;
  console.log(value, oldValue)
  if (value &#x3D;&#x3D; 5) stop()
&#125;)
&lt;&#x2F;script&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>情况二：监视ref定义的对象类型数据，这个有一个点需要注意。&#x3D;&#x3D;当我们需要对象内部某个属性值的时候，这时的新值和旧值是相同的，因为此时这里的值是通过对象的地址来进行获取的，只修改对象的属性是不会导致对象的地址发生改变的，所以改属性时，新旧值相同。同理的，当我们整体的去修改一个对象时，此时对象的地址就会发生变化，从而可以看到新旧值是不相同的。&#x3D;&#x3D;下面给出一个具体样例：</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">&lt;template&gt;
  &lt;div&gt;
    &lt;h1&gt;姓名:&#123;&#123; person.name &#125;&#125;&lt;&#x2F;h1&gt;
    &lt;h1&gt;年龄:&#123;&#123; person.age &#125;&#125;&lt;&#x2F;h1&gt;
    &lt;button @click&#x3D;&quot;changeName&quot;&gt;点我修改名称&lt;&#x2F;button&gt;
    &lt;button @click&#x3D;&quot;changeAge&quot;&gt;点我修改年龄&lt;&#x2F;button&gt;
    &lt;button @click&#x3D;&quot;changePerson&quot;&gt;点我改头换面&lt;&#x2F;button&gt;
  &lt;&#x2F;div&gt;
&lt;&#x2F;template&gt;
&lt;script setup name&#x3D;&quot;Person&quot;&gt;
import &#123;ref, watch&#125; from &quot;vue&quot;;

let person &#x3D; ref(&#123;
  name: &quot;张三&quot;,
  age: 18
&#125;)

function changeName() &#123;
  person.value.name &#x3D; &quot;李四&quot;
&#125;

function changeAge() &#123;
  person.value.age++
&#125;

function changePerson() &#123;
  person.value &#x3D; &#123;name: &quot;王五&quot;, age: 48&#125;
&#125;

watch(person, (newValue, oldValue) &#x3D;&gt; &#123;
  console.log(newValue, oldValue)
&#125;, &#123;deep: true&#125;)

&lt;&#x2F;script&gt;
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>
</li>
<li><p>情况三：监听reactive创建的对象类型的数据，这个和情况二的区别就是：&#x3D;&#x3D;reactive创建的对象的监听默认是开启了深层次监听的，而且我们是无法通过option进行关闭的。&#x3D;&#x3D;</p>
</li>
<li><p>监视ref或reactive定义的【对象类型】中的某个属性，注意以下内容：</p>
<ul>
<li>若该属性值是一个基本类型，需要写成函数类型，其实就是地址值。</li>
<li>若该属性值是一个对象类型，那么我们可以直接写，也可以写成函数，但是建议写成函数。</li>
</ul>
</li>
<li><p>监视多个数据</p>
<p>​	这里就是将多个内容放在一个数组里面，然后旧值和新值都是以数组的形式呈现。</p>
</li>
</ol>
<h1 id="watchEffect"><a href="#watchEffect" class="headerlink" title="watchEffect"></a>watchEffect</h1><p>​	这个内容和watch的作用很想，但是我们在使用这个时，并不需要指明要监视那些对象，他可以自己去分析需要监视那些属性。</p>
<h1 id="标签的Ref属性"><a href="#标签的Ref属性" class="headerlink" title="标签的Ref属性"></a>标签的Ref属性</h1><p>​	我们可以为每一个html元素或者一个组件添加ref标签，来确定的查看或者操作某个元素。</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">&lt;template&gt;
  &lt;div&gt;
    &lt;h1 ref&#x3D;&quot;h&quot;&gt;姓名&lt;&#x2F;h1&gt;
    &lt;button @click&#x3D;&quot;disPlay&quot;&gt;点我显示h1&lt;&#x2F;button&gt;
  &lt;&#x2F;div&gt;
&lt;&#x2F;template&gt;
&lt;script setup name&#x3D;&quot;Person&quot;&gt;
import &#123;ref&#125; from &quot;vue&quot;;

let h &#x3D; ref();

function disPlay()&#123;
  console.log(h.value)
&#125;

&lt;&#x2F;script&gt;
<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>​	不过需要注意的是，&#x3D;&#x3D;在标签上使用的ref需要和js或者ts中的变量命名是需要相同的。&#x3D;&#x3D;</p>
<p>当我们为一个组件使用ref时，在父组件中是无法看到子组件中js的内容的，除非子组件通过defineExpose来进行暴露。</p>
<h1 id="TS中的接口和范型"><a href="#TS中的接口和范型" class="headerlink" title="TS中的接口和范型"></a>TS中的接口和范型</h1><p>​	我们在自己定于数据时，很容易用着用着就会写错。所以我们可以使用ts的接口和范型来帮助我们。</p>
<p>ts中的接口和数据库定于一张表很像。定义完成之后将这个接口对外暴露即可。</p>
<pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">export</span> <span class="token keyword">interface</span> <span class="token class-name">Person</span> <span class="token punctuation">&#123;</span>
    id<span class="token operator">:</span><span class="token builtin">string</span><span class="token punctuation">,</span>
    name<span class="token operator">:</span><span class="token builtin">string</span><span class="token punctuation">,</span>
    age<span class="token operator">:</span><span class="token builtin">number</span>
<span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>​	如果我们需要让一个数组或者别的容器中的所有的内容都是这个<code>Person</code>类型的话就需要使用范型。其中范型的使用也有两种方式。</p>
<blockquote>
<ol>
<li>在我们的代码中直接使用范型例如</li>
</ol>
<pre class="line-numbers language-typescript" data-language="typescript"><code class="language-typescript"><span class="token keyword">let</span> persons<span class="token operator">:</span><span class="token builtin">Array</span><span class="token operator">&lt;</span>Person<span class="token operator">></span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token operator">...</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>

<p>这样就可以让persons中的每一个人都是符合我们自定义的Person的</p>
<ol start="2">
<li>在我们定义的types&#x2F;index.ts中进行定义</li>
</ol>
</blockquote>
<h1 id="vue3使用props进行参数传递"><a href="#vue3使用props进行参数传递" class="headerlink" title="vue3使用props进行参数传递"></a>vue3使用props进行参数传递</h1><p>​	这里可以有三种方式进行传递，如下所示：</p>
<blockquote>
<ol>
<li><p>只接收一个数据。具体情况如下所示</p>
</li>
<li><p>接收一个数据并且限制这个数据的类型。</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">父组件
&lt;template&gt;
  &lt;People :ren&#x3D;&quot;ren1&quot;&#x2F;&gt;
&lt;&#x2F;template&gt;
&lt;script setup name&#x3D;&quot;Person&quot; lang&#x3D;&quot;ts&quot;&gt;
import &#123;type Person&#125; from &quot;@&#x2F;types&quot;;
import People from &quot;@&#x2F;components&#x2F;People.vue&quot;;
let ren1: Person &#x3D; &#123;
  name: &quot;Eric1&quot;,
  age: 20,
  id: &quot;12345667789&quot;
&#125;
&lt;&#x2F;script&gt;

子组件
&lt;template&gt;
  &lt;h1&gt;姓名:&#123;&#123; ren.name &#125;&#125;&lt;&#x2F;h1&gt;
  &lt;h1&gt;年龄:&#123;&#123; ren.age &#125;&#125;&lt;&#x2F;h1&gt;
&lt;&#x2F;template&gt;
&lt;script lang&#x3D;&quot;ts&quot; setup&gt;
import type &#123;Person&#125; from &quot;@&#x2F;types&quot;;
defineProps&lt;&#123; ren: Person &#125;&gt;()
&lt;&#x2F;script&gt;
&lt;style scoped&gt;

&lt;&#x2F;style&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>我们可以看到，在父组件中传递时需要将<code>ren</code>这个添加上’:’，因为在vue中，加上这个就相当于去进行解析，而不是单纯的字符串。我们在子组件中进行接受的时候，如果要接受的属性必须是某个特定的类型的时候，我们需要给这个属性加上类型的约束（&#x3D;&#x3D;注意添加约束的时候需要写在范型里面，里面写了之后，外面就不需要再写了&#x3D;&#x3D;）</p>
</li>
<li><p>接收一个数据并且为这个数据加上一个默认值。</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">&lt;template&gt;
  &lt;h1&gt;姓名:&#123;&#123; ren.name &#125;&#125;&lt;&#x2F;h1&gt;
  &lt;h1&gt;年龄:&#123;&#123; ren.age &#125;&#125;&lt;&#x2F;h1&gt;
&lt;&#x2F;template&gt;
&lt;script lang&#x3D;&quot;ts&quot; setup&gt;
import type &#123;Person&#125; from &quot;@&#x2F;types&quot;;
   
withDefaults(defineProps&lt;&#123; ren?: Person &#125;&gt;(), &#123;
  ren: () &#x3D;&gt; (&#123;name: &quot;eric&quot;, age: 20, id: &quot;12345566778888676&quot;&#125;)
&#125;)
&lt;&#x2F;script&gt;
&lt;style scoped&gt;
   
&lt;&#x2F;style&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>

<p>​	这里有不少的坑，这个地方的书写相较于vue2麻烦了不止一点半点。我们这里需要使用<code>withDefaults</code>这个关键词来帮助我们设置默认值，而且默认值需要使用肩头函数返回一个对象，我试了好几次才找到返回一个对象的方法，要不就使用一个小括号包起来，要不就在大括号中写一个return然后再加上需要返回的对象即可。</p>
</li>
</ol>
</blockquote>
<h1 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h1><h2 id="vue2"><a href="#vue2" class="headerlink" title="vue2"></a>vue2</h2><img src="/2024/01/23/vue-ts%E5%AD%A6%E4%B9%A0/%E6%88%AA%E5%B1%8F2024-02-23%2023.44.01.png" class="">

<p>​	vue2主要是这个4个阶段，每个阶段都有两个过程，一个是处理之前，一个是处理完毕。</p>
<h2 id="vue3"><a href="#vue3" class="headerlink" title="vue3"></a>vue3</h2><img src="/2024/01/23/vue-ts%E5%AD%A6%E4%B9%A0/%E6%88%AA%E5%B1%8F2024-02-24%2000.15.36.png" class="">

<p>​	vue3生命周期的创建阶段时通过setup来完成了，而且我们在使用这个钩子函数的时候需要自己手动的引入这些函数才行。</p>
<p>&#x3D;&#x3D;注意，不管是vue2还是vue3都不止这几个钩子，还有其他的钩子，我们会在后面的学习中不断的遇到。&#x3D;&#x3D;</p>
<h1 id="自定义hooks"><a href="#自定义hooks" class="headerlink" title="自定义hooks"></a>自定义hooks</h1><p>​	我们可以自己定义对于不同数据的处理方法，并且把不同的属性和方法封装在一个文件中，形成一个useXXX的hook，我们只需要在需要使用的文件中进行调用即可。同时，这种方法也体现出了&#x3D;&#x3D;高内聚、低耦合&#x3D;&#x3D;的思想</p>
<h1 id="vue3路由"><a href="#vue3路由" class="headerlink" title="vue3路由"></a>vue3路由</h1><h2 id="路由使用的步骤"><a href="#路由使用的步骤" class="headerlink" title="路由使用的步骤"></a>路由使用的步骤</h2><p>​	在vue3中，路由使用是非常简单的。一共有一下几个步骤</p>
<blockquote>
<ol>
<li>我们划分好导航区、内容区</li>
<li>引入路由的内容，如RouterLink、RouterView等</li>
<li>编写路由的规则，确定好每个路由对应的组件</li>
<li>合起来</li>
</ol>
</blockquote>
<h2 id="两个注意点"><a href="#两个注意点" class="headerlink" title="两个注意点"></a>两个注意点</h2><blockquote>
<ol>
<li>注意区分路由组件和一般组件，路由组件一般放在pages和views里面，而一般组件通常存放在components里面。&#x3D;&#x3D;一般组件是直接书写名字来使用，而路由组件使用实时渲染来使用&#x3D;&#x3D;</li>
<li>路由组件通常在不使用的时候会被卸载掉，在使用时进行挂载。</li>
</ol>
</blockquote>
<h2 id="路由器的工作模式"><a href="#路由器的工作模式" class="headerlink" title="路由器的工作模式"></a>路由器的工作模式</h2><p>​	路由器的工作模式主要有两种，一种是history模式，一种是hash模式。</p>
<img src="/2024/01/23/vue-ts%E5%AD%A6%E4%B9%A0/%E6%88%AA%E5%B1%8F2024-02-24%2022.58.56.png" class="">

<p>&#x3D;&#x3D;SEO 是搜索引擎优化（Search Engine Optimization）的缩写，是一种通过优化网站内容、结构和其他相关因素，以提高网站在搜索引擎中排名和可见性的技术和策略。SEO 的目标是使网站在搜索引擎中获得更高的排名，从而增加网站的流量、提高用户的访问量和质量，进而达到增加销售、推广品牌或实现其他业务目标的目的。&#x3D;&#x3D;</p>
<h2 id="to的两种写法"><a href="#to的两种写法" class="headerlink" title="to的两种写法"></a>to的两种写法</h2><ul>
<li><p>第一种就是to后面添加一个字符串</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">&lt;RouterLink to&#x3D;&quot;&#x2F;home&quot;&gt;主目录&lt;&#x2F;RouterLink&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre>
</li>
<li><p>第二种写法如下</p>
<pre class="line-numbers language-vue" data-language="vue"><code class="language-vue">&lt;RouterLink :to&#x3D;&#123;path:&#39;&#x2F;home&#39;&#125;&gt;主目录&lt;&#x2F;RouterLink&gt;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li>
</ul>
]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
  <entry>
    <title>基于VScode和CMake进行c/c++开发</title>
    <url>/2024/01/22/%E5%9F%BA%E4%BA%8EVScode%E5%92%8CCMake%E8%BF%9B%E8%A1%8Cc-c-%E5%BC%80%E5%8F%91/</url>
    <content><![CDATA[]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
  <entry>
    <title>实习内容</title>
    <url>/2024/05/13/%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="网络"><a href="#网络" class="headerlink" title="网络"></a>网络</h1><ol>
<li>RSA握手的过程：</li>
</ol>
<blockquote>
<ul>
<li>首先，客户端想服务端发送一个ClientHello的报文，其中包括客户端的TLS的版本、一个随机数和客户端支持的加密算法列表。</li>
<li>服务端在受到这个报文之后，会向服务端发送3个报文，第一个为ServerHello，其中包括服务端TLS的版本、一个服务端生成的随机数和服务端选择的加密算法。第二个报文是发送服务端的数字证书。第三个报文发送一个HelloDone。</li>
<li>客户端受到信息后，会拿着服务端的数字证书，去CA查询是否正规。客户端的查询有一下几个步骤：通过CA的hash算法生成一个hash值（该数字证书的hash），服务端也会使用公钥对该数字证书进行解密生成一个hash。如果这两个hash相同，那么说明这个数字证书是有效的。<img src="https://cdn.xiaolincoding.com/gh/xiaolincoder/ImageHost4@main/%E7%BD%91%E7%BB%9C/https/%E8%AF%81%E4%B9%A6%E7%9A%84%E6%A0%A1%E9%AA%8C.png" alt="img"></li>
<li>完成认证之后，客户端会向服务端发送一个随意数（pre-master）用于生成会话的密钥（&#x3D;&#x3D;这个地方的密钥是通过之前选定的算法来的&#x3D;&#x3D;）</li>
<li>最后就是客户端和服务端之间进行通信，双方改成对称加密的方式进行通信</li>
</ul>
</blockquote>
<ol start="2">
<li>RSA的缺陷：</li>
</ol>
<blockquote>
<p>如果服务端的密钥被抄家了，那么就会出现信息的泄漏</p>
</blockquote>
<ol start="3">
<li><p>HTTPS可以如何优化</p>
<blockquote>
<p><code>缺点 </code> ：首先，在谈如何优化之前，我们应该知道他的缺点在哪里。我认为https有3处缺点：1.https的TSL握手时，会有随机数或者椭圆区县公私钥的生成；服务端数字证书的认证；和双方计算的pre-master随机数</p>
<p>方案：</p>
<ul>
<li>硬件优化，有钱的话，可以去买更好地cpu。因为在这个过程中，客户端和服务端会进行大量的运算，所以这是一个计算密集型的任务，压力给到cpu。尤其是可以购买AESni</li>
<li>软件优化，可以升级系统的内核和SSL之类的相关软件</li>
<li>协议优化：其实就是对密钥交换的过程进行优化就是从RSA密钥交换升级为ECDHE算法，该算法有两个优势，一个是可以实现数据发送的抢跑，即在第三次握手的时候就可以发送加密的应用数据；二是该算法具备前向安全性。</li>
<li>证书优化：1.可以使用ECDHE的证书，这个更短。2、可以使用ocsp+stapling作为验证证书是否失效的方法，常规的验证为CRL，客户端会去下载大量的列表，然后查询。而ocsp则是向CA服务器直接查询某个证书，而stapling是服务器的一种优化方式，服务器定时去查询ocsp，并且给自己的证书加上时间戳。这样客户端就不需要每次都去访问CA这个证书是否有效。</li>
<li>会话复用：有两种方式，一种是session id，就是把上次使用过得TSL握手保存下来，然后下次创建连接的时候，直接查，然后在内存中找到这个session id对应密钥。然后直接进行通信即可。另外一种就是类似于JWT的验证方式，让客户端来保存身份信息，每次进行链接的时候带上。如果在有效期内的话，就直接通过这个密钥进行通信即可。但是session ticket对于集群式的服务器不太有好，因为上次服务的不一定是这台机器，就可能导致重新连接。但是会话复用会有问题，如果有人劫持了这个</li>
</ul>
</blockquote>
</li>
<li><p>Http1.1的优化</p>
<blockquote>
<p>Http1.1相对于http1.0，只添加了一个长链接而已</p>
</blockquote>
</li>
<li><p>HTTP2.0的优化</p>
<blockquote>
<ol>
<li>头部压缩：http2添加了两种压缩的编码： &#x3D;&#x3D;静态表编码&#x3D;&#x3D;和&#x3D;&#x3D;动态表编码&#x3D;&#x3D;。其中静态表编码是将header中常用的一些字符通过huffman编码的方式进行压缩。我们在发送数据的时候配合着http2发送二进制的方式。可以大大减少http报文头部的长度。其中动态表编码是指将一个http报文的头部的某个字段进行编码，当下次再受到这个字段的时候就不需要额外的进行发送了。但是这种方式只适合于发送大量的头部相同的http报文。因为如果某个字段发生变化的话，这个编码的字段就不能用在当前的报文中。导致内存中存放的动态编码会越来越多。所以我们可以设置一个动态表编码的上限。</li>
<li>二进制帧： 二进制是从http2开始使用的，一个帧中需要有标志位用于记录当前的帧是否结束、以及当前帧所在流的优先级。同时一个帧中还有流标识符，用于标志当前链接中的流，而且客户端的流只能是奇数，服务端的流的id应该是偶数。&#x3D;&#x3D;每个帧中存放的数据应为一个http报文的header或者data（即payload）&#x3D;&#x3D;</li>
<li>并发传输：得益于帧和流的技术，http2能够实现一个tcp链接下并发的发送n个请求，并且我们可以根据自己的需求来给不同的流添加相应的优先级</li>
<li>服务端主动推送数据：因为有http2帧的存在，所以我们可以设置流的不同，将服务器给客户发送的消息加到不同的流里面。比如，我们可以让客户在请求html的时候，服务器自动的返回css的内容。这是就可以在客户端起的流里面返回一个服务器想启动的流的id，告诉客户端，接下来的服务器想给客户端的信息将通过这个流来进行发送。</li>
</ol>
</blockquote>
</li>
<li><p>http3的优点</p>
<blockquote>
<p>先说一下http2的问题：1. 对头阻塞，这里指的是TCP队头阻塞了，因为http2使用的是tcp，当发生丢包的时候，会进行重传，这个时候一些已经准备好的资源也会跟着进行等待。</p>
<pre><code>                2. 还是具有TCP的握手和TSL的握手延时
                2. 网络迁移需要重新进行连接：因为一个TCP链接是由四元组（源ip、目的ip\源端口、目的端口）组成的，所以当网络变化时，ip就会产生变化，导致TCP需要进行重新链接
</code></pre>
<p>http3所做的就是解决了http2中的这些问题。队头阻塞就使用了quic（quickly udp internet connection）协议。对于这个Tcp的握手和TSL的握手就是用链接ID来进行代替。</p>
</blockquote>
</li>
<li><p>对于http2和http3动态表编码的一些问题</p>
<blockquote>
<p>HTTP&#x2F;2 和 HTTP&#x2F;3 的 Huffman 编码并没有多大不同，但是动态表编解码方式不同。</p>
<p>所谓的动态表，在首次请求-响应后，双方会将未包含在静态表中的 Header 项更新各自的动态表，接着后续传输时仅用 1 个数字表示，然后对方可以根据这 1 个数字从动态表查到对应的数据，就不必每次都传输长长的数据，大大提升了编码效率。</p>
<p>可以看到，<strong>动态表是具有时序性的，如果首次出现的请求发生了丢包，后续的收到请求，对方就无法解码出 HPACK 头部，因为对方还没建立好动态表，因此后续的请求解码会阻塞到首次请求中丢失的数据包重传过来</strong>。</p>
<p>HTTP&#x2F;3 的 QPACK 解决了这一问题，那它是如何解决的呢？</p>
<p>QUIC 会有两个特殊的单向流，所谓的单向流只有一端可以发送消息，双向则指两端都可以发送消息，传输 HTTP 消息时用的是双向流，这两个单向流的用法：</p>
<ul>
<li>一个叫 QPACK Encoder Stream，用于将一个字典（Key-Value）传递给对方，比如面对不属于静态表的 HTTP 请求头部，客户端可以通过这个 Stream 发送字典；</li>
<li>一个叫 QPACK Decoder Stream，用于响应对方，告诉它刚发的字典已经更新到自己的本地动态表了，后续就可以使用这个字典来编码了。</li>
</ul>
<p>这两个特殊的单向流是用来<strong>同步双方的动态表</strong>，编码方收到解码方更新确认的通知后，才使用动态表编码 HTTP 头部。</p>
</blockquote>
</li>
<li><p>WebSocket和HTTP有什么联系</p>
<blockquote>
<p>这两个协议都是应用层的协议，底层都是使用的TCP来进行链接的。但是，HTTP只是用了TCP一半的功能，即客户向服务器发送请求。而websocket却是实实在在的实现了双向通信的功能。websocket的创建是通过HTTP来进行建立的。在HTTP连接的接触上，通过发送<code>Update:WebSocket</code>的字段，来实现协议的转换。</p>
<p>WebSocket之间的握手是这样的，客户端向服务器发送的这个升级的报文中有一个base64 编码的字符串，服务器在拿到这个字符串之后，用一个公开的算法对这个字符串进行计算得到一个新的字符串，然后返还给客户端。客户端受到这个字符串之后自己也运行一变这个公开的算法。看看二者得到的是不是相同的字符串，如果相同的话，那么二者之间就建立起了WebSocket链接。可以通过mask位来设置payload是否需要做掩码处理</p>
</blockquote>
</li>
<li><p>RPC和HTTP的关系</p>
<blockquote>
<p>RPC其实和HTTP的功能是比较类似的一类协议，比如有gRPC。其实设计RPC的初衷是为了让我们调用远程的某个服务能够像调用本地的某些资源一样方便。RPC主要是用于CS之间的通信。而HTTP主要是BS之间的。随着发展，所有端的基本上都采用了BS架构，所以现在一般RPC都是公司内部使用，访问某个固定的服务器的时候使用，RPC也有一些优势，比如说RPC有TCP的一个连接池。当我们需要向服务器请求什么的时候，可以从连接池中拿一个出来，使用完之后再将这个TCP链接放回去。而且RPC的可定制性非常高，不需要像HTTP那样传输很多的字段，我们可以根据自己的需要来选择传输什么字段的。</p>
</blockquote>
</li>
<li><p>TCP和UDP可以绑定到同一个端口吗？</p>
<blockquote>
<p>其实是可以的，因为TCP和UDP是传输层的两个协议，每个协议都有自己的端口号。每种协议都在内核中维护着。虽然当我们用了TCP和UDP相同的端口号，但是当一个数据包来到之后通过端口查找那个程序的时候，会在不同的协议之间进行查找，所以二者之间不会影响。</p>
</blockquote>
</li>
<li><p>TCP为什么是3次握手而不是2次或者4次</p>
<blockquote>
<p>TCP的3次握手有以下几个作用：1.验证客户端和服务端双方都能够进行接受和发送消息，比如，发送的第一个TCP可以验证客户端能发送，第二次握手可以验证服务器可以接受和发送，第三次握手可以验证客户端接受。2.实现客户端和服务端之间的序号初始化</p>
<p>那么三次握手为什么不是4次呢？原本是可以服务端给客户端发送的信息分成两条来发送，但是这两条信息的内容并不冲突（使用不同的标志位）所以可以一次性发送过来。所以只需要3次即可。</p>
<p>为什么不是2次握手呢？2次握手会有以下问题，就是客户端发送了一个syn包之后，网络阻塞或者客户端重启之后再次执行业务会发送相应的syn包。这时如果只有两次握手的话，那么很有可能就会建立很多了无效的连接，占用服务器的资源</p>
</blockquote>
</li>
<li><p>TCP报文中时间戳的作用</p>
<blockquote>
<ol>
<li><p>便于计算RTT</p>
</li>
<li><p>用于防止循环报文的问题（序列号回绕的问题）</p>
</li>
</ol>
</blockquote>
</li>
<li><p>IP层会分片，为什么还需要MSS</p>
<blockquote>
<p>我的理解是这样的，如果一个TCP报文直接交给ip层来进行分片的话，如果长度超过了MTU的话，那么就需要IP分片。这时TCP的首部就只有一个ip分片中有，当其中一部分丢失以后。TCP就无法获取整个完成的报文。所以需要重传整个报文。而如果TCP自己就进行分片的话，每个ip分片都一个TCP报文的头部。这样某个报文丢失时，只需要重传这一个报文就可以了。</p>
</blockquote>
</li>
<li><p>注意分析TCP握手时丢包后的问题</p>
<blockquote>
<p>每个握手时的丢包都会导致上一个的重传和自己的重传（如果上一个包有的话）。而且都是等待RTO的时间，每多一次重传，等待的时间就是上一次RTO的2倍。我们可以通过调节tcp_syn_retries和tcp_sycack_retries来控制重传报文的次数</p>
</blockquote>
</li>
<li><p>TCP 的四次回收可以优化为3次挥手吗？</p>
<blockquote>
<p>可以的，当服务器没有数据需要发送给客户端时。就可以实现优化。优化的原理是：<code>ACK延迟确认</code>：就是说，TCP为了减少网络压力，尽力将ACK放在有应用数据传递的报文中进行发送。所以，ACK会稍微等待一段时间来看看有没有数据需要返回，如果有的话就跟着回去。如果没有就自己返回。但是，如果客户端的第二条信息发送过来了，那么这个ACK必须立即返回。</p>
</blockquote>
</li>
<li><p>TCP连接中出现进程崩溃或者主机宕机时会发生什么？</p>
<blockquote>
<ol>
<li>我们需要分情况来进行讨论了。首先如果是进程崩溃的话，会正常的关闭TCP链接。因为TCP是由内核中来维护的，当进程崩溃时，内核会回收该进程的TCP链接资源，发送FIN报文，从而关闭链接。</li>
<li>如果是主机宕机了，这个时候，我们就要看二者之间有没有发送数据，如果二者之间发送了数据。那么就可以通过超时重传的机制来判断当前的链接是否是正常的（linux内核中有一个参数是用来控制超时重传的次数的tcp_retires2，如果重传的次数超过了这个值，那么就会判断当前的TCP链接出现了问题，从而中断TCP）</li>
<li>但是如果二者之间并没有数据交换的话，就要看TCP链接的keepalive又没有打开，如果这个选项打开了，二者在长时间没有进行通信之后，就会发送检测报文来判断当前的TCP链接是否失效，这个检测报文也有一个发送次数的上限和发送时间之间的间隔。</li>
<li>最后，如果keepalive没有开的话，那么就真的不行了。这个established的TCP连接会一致在内核中维护，知道服务端进行重启。</li>
</ol>
</blockquote>
</li>
<li><p>鹅厂TCP面试题，拔掉网线后，这个TCP链接还存在吗?</p>
<blockquote>
<p>其实这个问题和上面的那个进行崩溃和主机宕机很像。其实，&#x3D;&#x3D;拔掉网线就是一种宕机，就看这个期间又没有消息的传递和keepalive机制又没有开启&#x3D;&#x3D;</p>
</blockquote>
</li>
<li><p>全连接队列和半连接队列相关知识点</p>
<blockquote>
<ol>
<li><p>服务端在执行listen函数的时候，会创建一个<code>半连接队列</code>和<code>全连接队列</code>。这两个队列分别使用hash表和链表来进行存储。因为半连接队列是还没有完全建立的队列，他需要第三次握手之后才可以建立，所以使用hash可以在O（1）的时间内找到它。</p>
</li>
<li><p>我们可以使用ss -lnt来查看当前一些socket的连接信息，其中recv-q代表着使用了多少个连接，而send-q是最大有多少个全连接</p>
</li>
<li><p>全连接队列满了之后，再受到第三次握手的ACK时会直接丢弃。其中还有一个<code>tcp_abort_on_overflow</code>的参数可以控制需不需要重传等待全连接可以放下当前的连接请求。如果该字段设置为0，在丢弃ACK包之后，会重传SYN+ACK的第二次握手。如果重传超过限制的话，就会删除半连接队列中的连接。如果该参数设置为1，会直接给客户端发送一个RST报文。</p>
</li>
<li><p>半连接队列满了之后。一般情况下半连接队列是不会满的。除非受到SYN洪泛攻击时，会产生大量的半连接状态的TCP链接。这个时候如果满了一般情况下会直接丢弃。但是也可以通过开启<code>tcp——syncookies</code>字段来绕过半连接队列。具体的绕过方式理解如下：</p>
<p><code>服务端会根据发过来的请求的ip、时间戳等信息生成一个hash，然后将这个hash放到SYN+ACK的保重返还给客户端。只要客户端第三次返还会这个cookie的话，就确认建立连接。</code>从而通过这种方式来避免SYN的攻击。但是这样也产生了另外一个问题，因为服务器验证这个cookie是需要时间和算力的。如果当攻击者伪造了大量的ACK报文和随意的cookie时，会给服务器的CPU带来极大的压力。也可以达到攻击的目的。</p>
</li>
</ol>
</blockquote>
</li>
<li><p>TCP的流量控制和拥塞控制</p>
<blockquote>
<p>这两个作用的对象是不同的，流量控制主要是在发送端和接受端之间进行的。主要是用来防止双方在发送数据时，速度差异太大。而拥塞控制主要是用来限制发送短向网络发送的速度的，用来限制发送端突然向网络中塞入大量的数据造成网络的拥塞。</p>
<p>流量控制窗口关闭的问题：</p>
<p>​	在这个问题中，当接受端告诉发送短的窗口大小为0时，这时发送短会停止发送。知道接受端的接受窗口不再为0时会向发送端进行通知。但是如果这个时候通知的报文丢失了的话，就会造成发送端和接受端的死锁（都在等双方的消息）。所以，这个时候，我们可以让发送端在接受到窗口为0的报文时，就开启一个计时器，每过一段时间就去主动询问一次接受端是否可以接受了。（有的TCP实现在查询多次仍然为0时，就主动断开TCP链接）</p>
<p>流量控制糊涂窗口综合症：</p>
<p>​	这个问题就是，如果接受端很忙，每次刚接受了一小部分数据后，发送短就迫不及待的将一点点数据+很大的报文header发送给接受端。这样就会造成网络数据传输的效率很低。这个时候我们就可以通过限制双方来控制小报文的发送。</p>
<ul>
<li><p>接受端：当可以接受的窗口大小小于（MSS,缓存空间&#x2F;2）时，就通知发送短当前的发送窗口为0.</p>
</li>
<li><p>发送端：Nagle算法</p>
<blockquote>
<p>条件一：需要窗口大小&gt;&#x3D;MSS，并且数据大小&gt;&#x3D;MSS</p>
<p>条件儿：受到之前发送数据的ACK包</p>
</blockquote>
<p>只要这两个条件满足其中的一个，就可以直接发送。</p>
</li>
</ul>
</blockquote>
</li>
<li><p>TCP可以如何优化</p>
<blockquote>
<ul>
<li>客户端：<ul>
<li>因为客户端在报文超时会进行重传。所以，我们可以修改报文重传的次数（tcp_syn_retries）来修改和调整客户端的重传次数。从而调整客户端三次握手的上限。</li>
</ul>
</li>
<li>服务端：<ul>
<li>我们知道服务端是由两个队列来维护的，一个半连接队列一个全连接队列。当半连接队列满了之后，我们有以下处理得方法。<ul>
<li>调节半连接队列的大小。通过修改参数somaxconn和backlog、tcp_max_syn_backlog来调整accept 队列，进而增大半连接的队列。因为单纯的修改backlog是不起作用的。</li>
<li>或者我们可以使用syncookies来应对半连接队列满了的情况。当使用了syncookies时，收到的syn会直接返回并且加上一个根据TCP相关的信息计算出的cookies信息。然后发送给客户端。如果客户端发挥了，那么就直接直接建立链接。</li>
<li>对于全连接队列的问题。一般情况下我们会丢弃掉这个第三次握手的ACK包。这个时候syn+ack的第二次握手就会重新发送，知道超过syn+ack的超时重传的次数。&#x3D;&#x3D;这个是linux的默认操作&#x3D;&#x3D;</li>
<li>或者我们可以打开一个叫做tcp_abort_on_overflow的参数。当这个参数打开之后，如果全链接队列满了就会给客户端发送一个RST报文。告诉客户端断开链接。accept队列的长度&#x3D;min（somaxconn，backlog）。但是这样操作不太好。</li>
<li>或者我们可以使用seesion Tickets在TCP里面就叫做Fast open。我们第一次发送请求的时候，携带这个字段并且设置为0。第一次请iqude时候，服务端会返回。下次请求时只需要客户端携带这个字段，并消耗1RTT时间就可以完成链接。</li>
</ul>
</li>
</ul>
</li>
</ul>
</blockquote>
</li>
<li><p>TCP是如何处理粘包的问题的？</p>
<blockquote>
<p>先来说一说为什么会产生TCP粘包：因为Nagle算法的存在，小的报文会积累在一起，等到发送给接受端的时候，接受端就无法判断每个消息之间的边界了。这样就可能出现粘包的问题。或者服务器每次从缓冲区拿的太少，下次拿的时候也不知道在哪里断开。</p>
<ul>
<li>使用特殊的字符作为边界。这个其实已经在http中实际使用了。比如，http使用回车换行作为边界。但是当我们需要发送的消息里面包含了这个边界的时候，就会出现错误。</li>
<li>所以我们可以先定义一个结构体。这个结构体的长度我们先定义好比如说4个字节。我们下次读取的时候，可以先读取4个字节，让我们来获取这个结构体。然后我们在这个结构体中可以自己定义一个数据的长度和数据的集合。我们之后再根据这个长度来接受需要大小的数据即可。&#x3D;&#x3D;其实就是在应用程序的角度来帮助TCP连接进行处理了。&#x3D;&#x3D;</li>
</ul>
</blockquote>
</li>
<li><p>SYN报文丢弃的情况</p>
<blockquote>
<ol>
<li><p>如果我们开启了tcp_tw_recycle和timestamps。因为当开启了timestamps后，默认会开启一个叫做per-host<code>PAWS机制</code>：这个机制就是对对端IP进行PAWS检查，而不是对IP+端口检查。所以这个时候，如果发送端是一个NAT网络的话，这是就会发生大量的丢包行为。因为NAT 网络中的主机都有自己的始终，会产生不同的timestamps。所以，不同机器发送给服务端的报文会有不同的timestamps，而在一定的时间内，服务端只会接受唯一一个标记timestamps的报文。</p>
</li>
<li><p>如果半连接队列满了，这个时候不会接受SYN报文了。</p>
<p>我们该如何应对SYN洪泛攻击呢？</p>
<ol>
<li>增大半连接队列，三个参数tcp_max_syn_backlog,somaxconn,backlog</li>
<li>开启tcp_syncookies</li>
<li>减少SYN-ACK的重传次数</li>
</ol>
</li>
<li><p>如果全链接队列满了，也会导致半连接队列不接受SYN报文，导致丢失SYN包</p>
</li>
</ol>
</blockquote>
</li>
<li><p>如果已经建立的TCP受到SYN会发生什么</p>
<blockquote>
<p>如果一个建立的TCP再次收到SYN是，他会返回一个chanllge ACK（此时服务端想要接受的报文的序列号）。然后我们可以根据这个序列号来做一些不好的事情。如发送一个RST报文，断开链接等。</p>
<p>所以如果是客户端宕机了，再次启动的时候还使用这个端口于原来的服务建立连接的话。发送SYN之后就会受到challenge ACK。这个时候客户端发现回复的序列号不是自己想要的，于是就会返回一个RST断开这个链接。</p>
</blockquote>
</li>
<li><p>在TIME_WAIT状态的TCP链接，受到SYN后会发生什么？</p>
<blockquote>
<p>如果收到的是合法的就接着复用，建立连接。如果是不合法的就会发送服务端可以接受到的下一条数据。</p>
<p>如果是合法的就会跳过2*MSL的等待时间。如果是非法的，客户端受到之后就会会RST给服务器。但是当服务器受到这个RST之后有两种操作，一种是丢弃该RST报文，另外一种是直接断开链接。不过比较好的做法是丢弃RST报文。因为挥手的wait——time时间是一种比较友好的操作。</p>
</blockquote>
</li>
<li><p>tcptwreuse默认关闭的原因</p>
<blockquote>
<p>第一种情况：虽然reuse+timestamps能够阻拦很多过期报文，但是不会阻拦RST报文。所以上次链接中如果有被网络阻塞的RST来到了下一次快速重用的TCP链接中时，可能会导致重用的TCP被断开。</p>
<p>第二种情况：如果第四次回收丢失了，且这时客户端发送了快速重用，这个时候客户端发送SYN，而服务端会的是challenge ACK，这个是否客户端就会会一个RST断开。也会导致重用的失败。</p>
</blockquote>
</li>
<li><p>TCP的一些缺陷</p>
<blockquote>
<ol>
<li>升级比较困难</li>
<li>TCP存在对头阻塞</li>
<li>网络迁移时靴靿重新链接</li>
</ol>
</blockquote>
</li>
<li><p>QUIC</p>
<blockquote>
<p>下面我们来详细的扒一下QUIC</p>
<p>首先，QUIC的报文是什么样的？一个QUIC报文有三层，分别是Packet 、Frame和http3frame</p>
<p>其中，Packet 中的header有两种，一种是Long Packet Header ，另外一种是Short Packet Header 。其中long的是用于首次建立连接。而Short是用于日常传输数据的。其中QUIC也许要进行三次握手用于协商出链接的ID。</p>
<p>而Short Packet Header中使用Packet Number是一个独一无二的编号，而且是严格递增的。所以就算是N号报文丢失了，重传的报文的编号也不会是N，而是一个比N大的值。&#x3D;&#x3D;这样做的好处，我们可以精确的计算出原始报文和重传报文的RTT，以此来计算的RTO会比较精确，从而降低重传的概率。&#x3D;&#x3D;</p>
<p>而一个Packet下面可以存放多个QUIC frame。每一个frame都有一个类型，例如当前的frame是stream类型的。<img src="/2024/05/13/%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%AC%94%E8%AE%B0/image-20240520161839248.png" class="" title="image-20240520161839248"></p>
<img src="/2024/05/13/%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%AC%94%E8%AE%B0/image-20240520161856960.png" class="">



<p>这里的stream id和http2 中的stream id作用是相同的。可以看成是一个TCP链接中的不同的http请求在公用一条TCP链接。offset就类似于TCP链接中的seq序号，用于保证数据的顺序行和可靠性。而length之名了frame数据的长度。</p>
<p>这里说一下，我们是如何保证数据的超时重传的。我们是根据一个Packet保中某个frame的stream id和offset来判读当前数据是否是前面丢失的某个数据。&#x3D;&#x3D;从而可以实现QUIC的Packet Number+Stream ID和offset的组合实现乱序的作用。&#x3D;&#x3D;</p>
<p>这里再说一下，QUIC是如何解决对头的问题的。是因为QUIC为每一个stream都单独分配了一个滑动窗口，使得一个链接上的多个stream之间没有依赖关系，相互独立。</p>
<p>QUIC 的流量控制</p>
<p>quic一共有两种流量控制的方法：</p>
<p>第一种是针对每一个stream进行限制：他的接受窗口的是由最大的偏移来决定的。如果受到了新的字段之后，就可以可以将接受窗口的左边界向右移动。而右边界的移动需要看已经被接受的数据有没有达到最大接受窗口的一半。如果已经达到了一半，那么就移动有窗口扩大我们的可用接受窗口。（移动的距离就是已经接受了的距离）</p>
<p>第二种方法就是把所有的stream可以使用的空间作为标准。整体的可以使用的就是这些。不过，我感觉这样做可能会导致饥饿的出现。</p>
</blockquote>
</li>
<li><p>如果服务端只绑定了端口和IP没有listen，这个时候服务端进行访问的话。服务端会返回一个RST报文，要求客户端关闭这个链接。</p>
</li>
</ol>
<h1 id="操作系统"><a href="#操作系统" class="headerlink" title="操作系统"></a>操作系统</h1><ol>
<li>epoll底层原理</li>
</ol>
<blockquote>
<p>epoll底层实现是使用了一个红黑树来维护当前的socket事件，并且这个红黑树是直接放在内核中的。所以每次添加或者删除一个监听对象时，不需要从用户空间复制到内核空间，这样就可以节省不少的时间。而且，当一个socket事件发生的时候，epoll会把这些时间组织成一个链表，当有socket事件发生时将链表返还给用户处理。</p>
<p>这种方式是通过一个通知的形式来进行调用的</p>
</blockquote>
<ol start="2">
<li>select和poll</li>
</ol>
<blockquote>
<p>这两个实现的方法差不多，都是在用户内存中进行维护，当需要检查那些socket事件发生时，就copy到内核中进行检查。内核会对这个数组或者bitmaps进行标记，然后再copy回用户空间，应用程序再扫一遍进行处理。</p>
<p>其中select是通过一个bitmap来对应socket文件描述符的，而poll是使用链表的形式来进行维护的。</p>
<p>就是再有时间发生的时候去进行调用呀</p>
</blockquote>
<ol start="3">
<li><p>常见的io模式</p>
<blockquote>
<ul>
<li>BIO（blocking io），当一个进程在请求某个资源的时候，如果该资源没有立即的返回，那么这个进程就会将自己阻塞并且等待资源的返回。直到当资源准备好并且被从内核拷贝到用户内存之后才会被唤醒。&#x3D;&#x3D;突出一个阻塞&#x3D;&#x3D; 就导致当前的内核级现成无法进行工作了，如果是单核cpu的话，只能等待资源的返回，cpu才可以做其他的事情。</li>
<li>NIO（Non-blocking io ），当一个进程在请求某个资源的时候，如果该资源没有立即返回。那个这个进程并不会将自己阻塞，而是会不断的询问资源好了没有。期间cpu也可以做一些别的事情。直到资源准备好之后，进程会阻塞以完成资源从内核到用户内存的拷贝。&#x3D;&#x3D;虽然表面上是不阻塞，但是在资源进行复制的时候还是会阻塞的&#x3D;&#x3D;</li>
<li>io多路复用，这个线程可以监听多个socket请求，然后在调用select把当前的进程给阻塞。这个时候内核会监听select中所有的socket请求，当有一个请求的资源准备好后，select会返回，然后再从内核中进行数据的拷贝。</li>
<li>AIO（异步io），当一个进程请求了某个数据之后，就会立即返回。直到这个请求的数据准备完成并且从内核复制回用户空间之后就发送信号通知进程该请求已经完成。&#x3D;&#x3D;真正的实现了异步非阻塞&#x3D;&#x3D;</li>
</ul>
</blockquote>
</li>
</ol>
<h1 id="Golang基础"><a href="#Golang基础" class="headerlink" title="Golang基础"></a>Golang基础</h1><ol>
<li>channel使用的场景</li>
</ol>
<blockquote>
<ul>
<li>数据传递和分享</li>
<li>信号通知（我们可以使用一个传递空结构体的channel作为一个信号传递）</li>
<li>实现同步、锁的功能（只有在无缓冲区的模式下）</li>
</ul>
</blockquote>
<ol start="2">
<li>SingleFlight是什么？</li>
</ol>
<blockquote>
<p>SingleFlight是Go提供的一个扩展并发原语。</p>
<p>作用：在处理 多个goroutine同时调用同一个函数的时候，只让一个goroutine区调用这个函数，等这个goroutine返回结果时，再把这个结果返回给这个几个goroutines，这样可以减少并发调用的数量。</p>
<p>与sync.Once的区别：</p>
<ul>
<li>Sync.Onec是会保证永远只执行一次，而SingleFlight是每次调用的时候都会执行，并且在多个请求间执行一次</li>
<li>sync.Once主要是用于单词初始化场景中，而SingleFlight主要用在合并请求的场景中。</li>
</ul>
</blockquote>
<ol start="3">
<li>go语言的特点有哪些？</li>
</ol>
<blockquote>
<ul>
<li>channel，go中的channel是非常强大的，他可以实现进程间通信。这个方式和共享内存的方式是不同的。共享内存是能够直接改变数据的内容的，当一个进程修改之后，别的进程也是能够看到的，而channel不可以，除非传递的这个值是指针，并且修改了指针中的值。</li>
<li>goroutine ：go的goroutine是一种用户层的线程。当goroutine配合上我们的channel就可以实现不同携程之间同步的问题，因为channel会在通讯双方准备就绪之前进行阻塞，直到双方都准备就绪。</li>
<li></li>
</ul>
</blockquote>
<ol start="4">
<li>go channel的各种情况</li>
</ol>
<table>
<thead>
<tr>
<th align="center"></th>
<th align="center">关闭</th>
<th align="center">未初始化</th>
<th align="center">正常</th>
</tr>
</thead>
<tbody><tr>
<td align="center">发送</td>
<td align="center">panic</td>
<td align="center">死锁</td>
<td align="center">正常</td>
</tr>
<tr>
<td align="center">接受</td>
<td align="center">如果有数据接受数据，没数据会接受到0值</td>
<td align="center">死锁</td>
<td align="center">正常</td>
</tr>
<tr>
<td align="center">关闭</td>
<td align="center">Panic</td>
<td align="center">Panic</td>
<td align="center">正常</td>
</tr>
</tbody></table>
<ol start="5">
<li><p>init函数相关问题</p>
<blockquote>
<ul>
<li>每个文件中可以包含多个init函数，会按照init函数的顺序执行</li>
<li>不同包中的init按照包导入的依赖关系决定该如何初始化函数的执行顺序</li>
<li>同一个package下是根据文件名字符串大小比较来决定init（）的执行顺序</li>
<li>不同包就看谁先import</li>
</ul>
</blockquote>
</li>
<li><p>内存对齐的问题</p>
<blockquote>
<p>因为cpu访问内存时，是按照cpu的字长来进行访问的，所以当我们对数据进行内存对齐时，有利于cpu更好的提取数据，例如：如果一个数据横跨两个cpu字长的话，那么他需要2个cpu时刻才可以读取到完整的数据。所以我们需要对齐。在对一个结构体对齐时，我们可以通过unsafe保重的alignof（）函数查看当前结构体应该每个字段设置为多长。</p>
<p>&#x3D;&#x3D;对于空struct{}&#x3D;&#x3D;，一般是不需要考虑内存对齐的，因为他的大小为0。但是如果struct{}作为一个结构体的最后一个字段的话，那么就需要考虑了，需要补充一个额外的空间，来保证指针的安全</p>
</blockquote>
</li>
<li><p>字符串拼接各种方法的性能</p>
<blockquote>
<p><code>+</code>  ：当我们使用&#x3D;&#x3D;+&#x3D;&#x3D;进行字符串拼接的时候，因为字符串的值是不可以更改，因为string底层是一个指针和长度。所以，这个时候就会创建一个新的字符串用于保存当前的拼接的内容。当操作的次数提升之后，会严重影响性能。</p>
<p><code>Sprintf</code> :这个函数主要是用与格式化字符串，在源码中，他不仅复制了一次，而且逻辑非常的复杂。</p>
<p><code>strings.builder</code> ：这个方法是目前效率最高的方法。首先是他的内存分配比直接使用切片数组好一点，超过了2048之后就会变得缓慢（但是现在1.18之后切片的扩容发生发生了变化，不知道现在的效率是怎么样的）</p>
<p><code>byte.builder</code> ：这个相对于strings.builder多了一个[]byte-&gt;string 转换的过程，所以会慢一些</p>
</blockquote>
</li>
<li><p>for和range的性能差别</p>
<blockquote>
<p>首先我们要知道for和range都是如何来实现的，range其实是返回一个当前数据的副本，如果我们在切片中存的数据不是特别大的情况下，for和range的性能是差不多的。但是当这个元素的大小变大时，就会导致range拷贝消耗大量时间，从而降低性能。</p>
</blockquote>
</li>
<li><p>空结构体的作用</p>
<blockquote>
<ul>
<li>用作集合的实现</li>
<li>用作通道间的信号量</li>
<li>仅包含方法的结构体</li>
</ul>
</blockquote>
</li>
<li></li>
</ol>
<h1 id="数据库的内容"><a href="#数据库的内容" class="headerlink" title="数据库的内容"></a>数据库的内容</h1><h2 id="MySQL"><a href="#MySQL" class="headerlink" title="MySQL"></a>MySQL</h2><ol>
<li><p>MySQL锁</p>
<blockquote>
<p>Mysql的元数据锁，是在执行sql语句时，自动添加的。而是读读不互斥，读写和写写是互斥的。而且&#x3D;&#x3D;写锁的优先级是比读锁的优先级高。&#x3D;&#x3D;所以如果一个想拿到写锁的事务一直没有被执行的话，也会被block住</p>
</blockquote>
</li>
<li><p>InnoDB和MyISAM的区别</p>
<blockquote>
<ol>
<li>InnoDB是可以加行级锁的，而MyISAM不可以加行级锁。这就是InnoDB支持事务而MyISAM不支持事务的原因之一。</li>
<li></li>
</ol>
</blockquote>
</li>
</ol>
<h1 id="简历上提到的问题"><a href="#简历上提到的问题" class="headerlink" title="简历上提到的问题"></a>简历上提到的问题</h1><ol>
<li><p>有状态和无状态的区别？</p>
<blockquote>
</blockquote>
</li>
</ol>
]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
  <entry>
    <title>每日一句</title>
    <url>/2024/03/12/%E6%AF%8F%E6%97%A5%E4%B8%80%E5%8F%A5/</url>
    <content><![CDATA[<p><code>2024.3.12</code></p>
<blockquote>
<p>​	你跑的快，22岁有个家，身边全是赞叹。你跑的慢，30岁还在路上追求梦想。有的人为了车房拼了一辈子，有些人买辆摩托车走遍大好河山。</p>
<p>​	你想成为怎样的人，过怎样的生活，只要你不后悔就行，并不是所有人都能在早上七点钟起床的，也别拿一碗饭来衡量一个人胃口的大小。</p>
<p>​	有的人喜欢狼吞虎咽，有的人喜欢细嚼慢咽，允许别人做别人，也允许自己做自己，一岁有一岁的味道，跟着自己的心就好。</p>
<p>​	不是所有选择都必须要做出正确选项的，只要你想，就可以选择你喜欢的选项。沿途的花会一直开，以后的路也是，祝你也祝我。</p>
</blockquote>
]]></content>
      <tags>
        <tag>daily</tag>
      </tags>
  </entry>
  <entry>
    <title>软件体系结构复习</title>
    <url>/2024/01/01/%E8%BD%AF%E4%BB%B6%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84%E5%A4%8D%E4%B9%A0/</url>
    <content><![CDATA[<ol>
<li><p>什么是非功能性需求以及非功能性需求有哪些？</p>
<blockquote>
<p>非功能需求是与系统的运行、性能、可靠性等相关的需求，通常不直接涉及系统功能，但对系统的整体质量和性能有着重要影响。以下是一些常见的非功能需求：</p>
<ol>
<li><strong>性能需求：</strong> 包括系统的响应时间、吞吐量、并发用户数等。例如，系统需要在500毫秒内响应用户请求。</li>
<li><strong>可用性需求：</strong> 描述系统的可靠性和可用性，通常以百分比或时间表达。例如，系统每年至少可用99.9%的时间。</li>
<li><strong>可维护性和可扩展性需求：</strong> 系统的易维护性和可扩展性。例如，系统应该容易进行修改和添加新功能。</li>
<li><strong>安全性需求：</strong> 包括对数据的保护、访问控制、身份验证等方面的需求。例如，用户敏感数据必须加密存储。</li>
<li><strong>兼容性需求：</strong> 描述系统与其他软硬件组件或系统的集成程度。例如，系统需要与特定浏览器兼容。</li>
<li><strong>可靠性和容错性需求：</strong> 描述系统在面对错误或故障时的表现。例如，系统应该能够自动从硬件故障中恢复。</li>
<li><strong>可测试性需求：</strong> 描述系统易于测试的程度，包括单元测试、集成测试和系统测试。例如，系统应该支持自动化测试。</li>
<li><strong>用户体验需求：</strong> 描述系统的用户界面、易用性和用户满意度。例如，系统的界面应该直观、易学易用。</li>
<li><strong>法规和标准需求：</strong> 描述系统需要符合的法规和标准，例如数据保护法规或行业标准。</li>
<li><strong>性能需求：</strong> 包括系统的响应时间、吞吐量、资源利用率等。例如，系统需要在高并发情况下保持稳定的响应时间。</li>
<li><strong>资源需求：</strong> 描述系统对硬件、软件和其他资源的需求。例如，系统需要至少2GB的内存。</li>
</ol>
<p>这些非功能需求对于确保系统的整体性能、安全性和用户满意度至关重要。在项目开发的早期阶段就明确定义和考虑这些需求，有助于避免后期的问题和提高系统的质量。</p>
</blockquote>
</li>
<li><p>4+1视图内容，以及结合uml的情况？</p>
<img src="/2024/01/01/%E8%BD%AF%E4%BB%B6%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84%E5%A4%8D%E4%B9%A0/%E6%88%AA%E5%B1%8F2024-01-01%2011.42.05.png" class="">

<ol>
<li><p>用例视图</p>
<blockquote>
<p>作用：描述系统的功能需求，找出用例和执行者</p>
<p>使用对象：客户、分析者、设计者、开发者和测试者</p>
<p>使用的图：&#x3D;&#x3D;用例图、交互图、活动图和状态图&#x3D;&#x3D;</p>
<p>重要性：系统的中心，它决定了其他视图的开发，用于确认和最终验证系统。</p>
</blockquote>
</li>
<li><p>逻辑视图</p>
<blockquote>
<p>作用：描述如何实现系统内部的功能 ；</p>
<p>适用对象：分析者、设计者、开发者 ；</p>
<p>描述使用的图：&#x3D;&#x3D;类图和对象图、状态图、顺序图、协作图和活动图&#x3D;&#x3D; ；</p>
<p>重要性：描述了系统的静态结构和因发送消息而出现的动态协作关系。</p>
</blockquote>
</li>
<li><p>实现视图</p>
<blockquote>
<p>作用：描述系统代码构件组织和实现模块，及它们之间的依赖关系；</p>
<p>适用对象：设计者、开发者和测试者；</p>
<p>描述使用的图：&#x3D;&#x3D;构件图&#x3D;&#x3D;；</p>
<p>重要性：描述系统如何划分软件构件，如何进行编程。</p>
</blockquote>
</li>
<li><p>进程视图</p>
<blockquote>
<p>作用：描述系统的并发性，并处理这些线程间的通信和同步。展示与系统处理性能相关的主要元素，包括可伸缩性、吞吐量、基本时间性能。（考虑分布式系统）</p>
<p>适用对象：开发者和系统集成者；</p>
<p>描述使用的图：&#x3D;&#x3D;状态图、顺序图、合作图、活动图、构件图和配置图&#x3D;&#x3D;；</p>
<p>重要性：将系统分割成并发执行的控制线程及处理这些线程的通信和同步。</p>
</blockquote>
</li>
<li><p>配置视图</p>
<blockquote>
<p>作用：描述系统的物理设备配置，如计算机、硬件设备以及它们相互间的连接；</p>
<p>适用对象：开发者、系统集成者和测试者；</p>
<p>描述使用的图：&#x3D;&#x3D;配置图&#x3D;&#x3D;；</p>
<p>重要性：描述硬件设备的连接和哪个程序或对象驻留在哪台计算机上执行。</p>
</blockquote>
</li>
</ol>
</li>
<li><p>架构风格</p>
<p><a href="https://blog.csdn.net/weixin_47872288/article/details/133921088">https://blog.csdn.net/weixin_47872288/article/details/133921088</a></p>
</li>
<li><p>质量属性、策略</p>
<blockquote>
<p>质量属性是衡量软件系统特定方面质量的特征，它们通常包括性能、可靠性、可维护性、安全性等。为了满足这些质量属性，软件架构师采用不同的设计策略和决策。以下是一些常见的质量属性及相应的设计策略：</p>
<ol>
<li><strong>性能（Performance）：</strong><ul>
<li><strong>策略：</strong> 使用缓存技术、优化数据库查询、采用并发和并行处理、使用负载均衡器、采用异步处理等，以提高系统响应时间和吞吐量。</li>
</ul>
</li>
<li><strong>可靠性（Reliability）：</strong><ul>
<li><strong>策略：</strong> 实现冗余和备份机制、使用事务管理、设计容错机制、定期进行系统备份、监控系统状态以及采用健壮的错误处理。</li>
</ul>
</li>
<li><strong>可维护性（Maintainability）：</strong><ul>
<li><strong>策略：</strong> 使用模块化和松散耦合的设计、采用设计模式、提供清晰的文档、实施自动化测试、使用版本控制系统、遵循编码标准。</li>
</ul>
</li>
<li><strong>安全性（Security）：</strong><ul>
<li><strong>策略：</strong> 实施身份认证和授权、使用加密技术保护数据传输和存储、进行安全审计、定期进行漏洞扫描、更新系统和依赖项。</li>
</ul>
</li>
<li><strong>可用性（Availability）：</strong><ul>
<li><strong>策略：</strong> 设计冗余系统组件、实施负载均衡、采用灾难恢复计划、监控系统性能并进行实时故障检测。</li>
</ul>
</li>
<li><strong>可扩展性（Scalability）：</strong><ul>
<li><strong>策略：</strong> 使用水平和垂直扩展策略、采用微服务架构、使用云服务提供商的弹性计算资源。</li>
</ul>
</li>
<li><strong>可移植性（Portability）：</strong><ul>
<li><strong>策略：</strong> 使用跨平台的技术和框架、避免平台特定的依赖、尽量使用标准化的编程语言和库。</li>
</ul>
</li>
<li><strong>灵活性（Flexibility）：</strong><ul>
<li><strong>策略：</strong> 采用可配置的组件、使用插件架构、实施参数化设计、避免硬编码。</li>
</ul>
</li>
</ol>
</blockquote>
</li>
<li><p>设计模式：</p>
<ul>
<li><p>oo设计原则</p>
<blockquote>
<ol>
<li><strong>单一职责原则（Single Responsibility Principle - SRP）：</strong><ul>
<li><strong>原则：</strong> 一个类应该只有一个引起变化的原因。</li>
<li><strong>优点：</strong> 提高类的可维护性，降低变更引起的风险。</li>
<li><strong>缺点：</strong> 可能会导致类的数量增加。</li>
<li><strong>适用场景：</strong> 当一个类负责的功能过于庞大，难以管理时。</li>
</ul>
</li>
<li><strong>开闭原则（Open&#x2F;Closed Principle - OCP）：</strong><ul>
<li><strong>原则：</strong> 软件实体（类、模块、函数等）应该对扩展开放，对修改关闭。</li>
<li><strong>优点：</strong> 提高系统的可扩展性，降低修改带来的风险。</li>
<li><strong>缺点：</strong> 需要一定的抽象设计，增加系统的复杂性。</li>
<li><strong>适用场景：</strong> 在系统需要频繁变化的地方，通过扩展而不是修改来适应变化。</li>
</ul>
</li>
<li><strong>里氏替换原则（Liskov Substitution Principle - LSP）：</strong><ul>
<li><strong>原则：</strong> 子类必须能够替换其基类而不影响程序的正确性。</li>
<li><strong>优点：</strong> 提高代码的可维护性和可扩展性。</li>
<li><strong>缺点：</strong> 需要对继承关系进行深入理解和设计。</li>
<li><strong>适用场景：</strong> 在有继承关系的场景中，确保子类可以完全替代基类。</li>
</ul>
</li>
<li><strong>依赖倒置原则（Dependency Inversion Principle - DIP）：</strong><ul>
<li><strong>原则：</strong> 高层模块不应该依赖于低层模块，二者都应该依赖于抽象；抽象不应该依赖于细节，细节应该依赖于抽象。</li>
<li><strong>优点：</strong> 降低模块之间的耦合性，提高系统的可维护性和可扩展性。</li>
<li><strong>缺点：</strong> 需要引入抽象层次，增加设计的复杂性。</li>
<li><strong>适用场景：</strong> 在模块之间存在依赖关系的场景中，通过抽象解耦。</li>
</ul>
</li>
<li><strong>接口隔离原则（Interface Segregation Principle - ISP）：</strong><ul>
<li><strong>原则：</strong> 客户端不应该强制依赖于它们不使用的接口。</li>
<li><strong>优点：</strong> 提高接口的灵活性和可维护性。</li>
<li><strong>缺点：</strong> 可能会导致接口的数量增加。</li>
<li><strong>适用场景：</strong> 当一个接口过于庞大，客户端只使用其中一部分功能时。</li>
</ul>
</li>
</ol>
</blockquote>
</li>
<li><p>创建型模式</p>
<ul>
<li><p>工厂模式</p>
<blockquote>
<p>工厂模式主要包含以下几种形式：</p>
<ol>
<li><strong>简单工厂模式（Simple Factory Pattern）：</strong><ul>
<li>在简单工厂模式中，有一个工厂类，根据传递给它的参数，动态决定应该创建哪个产品类的实例。</li>
<li>这种模式违反了开闭原则，因为每次添加新产品都需要修改工厂类。</li>
</ul>
</li>
<li><strong>工厂方法模式（Factory Method Pattern）：</strong><ul>
<li>在工厂方法模式中，定义一个创建对象的接口，但让实现这个接口的类来决定实例化哪个类。</li>
<li>每个产品都有一个对应的工厂类，客户端通过调用工厂接口来创建产品。</li>
<li>这种模式符合开闭原则，因为添加新产品只需要添加新的工厂类。</li>
</ul>
</li>
<li><strong>抽象工厂模式（Abstract Factory Pattern）：</strong><ul>
<li>抽象工厂模式提供一个接口，用于创建相关或依赖对象的家族，而不需要指定具体类。</li>
<li>客户端使用产品的抽象接口，而不是直接使用具体的实现。</li>
<li>这种模式有助于确保一组相关的产品可以一起使用，并支持产品家族的替换。</li>
</ul>
</li>
</ol>
<p><strong>优点：</strong></p>
<ul>
<li>封装了对象的创建过程，降低了系统的耦合性。</li>
<li>提高了代码的可维护性和可扩展性。</li>
<li>符合开闭原则，对扩展开放，对修改关闭。</li>
</ul>
<p><strong>缺点：</strong></p>
<ul>
<li>引入了额外的类，增加了系统的复杂性。</li>
<li>在增加新产品时，可能需要修改工厂类的代码，违反了开闭原则（特别是简单工厂模式）。</li>
</ul>
<p><strong>适用场景：</strong></p>
<ul>
<li>客户端不关心对象的创建过程，只关心对象的接口。</li>
<li>系统中的一组相关对象需要一起使用，并且希望将它们的创建和使用分离。</li>
<li>需要提供一个统一的接口来创建一组相关或相互依赖的对象。</li>
</ul>
</blockquote>
</li>
<li><p>单例模式</p>
<blockquote>
<p>单例模式是一种创建型设计模式，其主要目的是确保类只有一个实例，并提供全局访问点。这对于需要在系统中的多个部分共享某些资源或状态的情况非常有用。</p>
<p><strong>优点：</strong></p>
<ol>
<li><strong>全局唯一性：</strong> 保证一个类只有一个实例，确保全局唯一访问点。</li>
<li><strong>延迟实例化：</strong> 仅在需要时创建实例，提高性能。</li>
<li><strong>节省资源：</strong> 由于只有一个实例，节省了系统资源。</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li><strong>全局状态：</strong> 单例模式引入了全局状态，可能对测试造成影响。</li>
<li><strong>滥用：</strong> 如果过度使用单例模式，可能会导致系统中的耦合问题。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li><strong>需要全局唯一性的对象：</strong> 例如配置管理、日志管理等。</li>
<li><strong>资源共享：</strong> 在整个系统中共享资源，避免多次创建相同的对象。</li>
</ol>
</blockquote>
</li>
<li><p>建造者模式</p>
<blockquote>
<p>建造者模式是一种创建型设计模式，旨在将一个复杂对象的构建与其表示分离，使得同样的构建过程可以创建不同的表示。</p>
<p><strong>优点：</strong></p>
<ol>
<li><strong>分离构建过程：</strong> 允许你改变一个产品的内部表示，同时不改变构建过程。</li>
<li><strong>更好的控制：</strong> 可以更好地控制构建过程，根据需要构建不同的对象。</li>
<li><strong>简化构建过程：</strong> 客户端代码只需指定需要构建的类型和内容，而无需关心具体的构建过程。</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li><strong>代码量增加：</strong> 引入了新的构建者类和指挥者类，可能导致代码量的增加。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li><strong>复杂对象的构建：</strong> 当一个对象的构建过程比较复杂，而且构建过程需要独立于其表示时，可以使用建造者模式。</li>
<li><strong>需要更好的构建控制：</strong> 当需要更好地控制构建过程，或者需要构建不同表示的对象时，建造者模式是一个不错的选择。</li>
</ol>
<p>注意：</p>
<p>&#x3D;&#x3D;工厂模式关注创建对象的过程，主要解决对象实例化的封装问题。&#x3D;&#x3D;</p>
<p>&#x3D;&#x3D;建造者模式关注创建复杂对象的过程，主要解决构建过程的复杂性和灵活性问题。&#x3D;&#x3D;</p>
<p>这个地方可以去想一下MC中的那个ButtonWidge的创建，那个就是使用builder进行创建的。</p>
</blockquote>
</li>
<li><p>原型模式</p>
<blockquote>
<p>原型模式是一种创建型设计模式，其主要目的是通过复制现有对象来创建新的对象，而无需直接使用其构造函数。这种模式涉及创建新对象的过程，通过克隆（复制）现有对象而不是通过实例化新对象来完成。</p>
<p><strong>主要角色和结构：</strong></p>
<ol>
<li><strong>原型接口（Prototype Interface）：</strong> 定义了克隆方法的接口，通常包含一个 <code>clone</code> 方法。</li>
<li><strong>具体原型类（Concrete Prototype）：</strong> 实现了原型接口的具体类。该类的实例可以通过克隆创建新的实例。</li>
<li><strong>客户端（Client）：</strong> 使用原型模式的客户端，通过克隆现有对象来创建新的对象。</li>
</ol>
<p><strong>工作原理：</strong></p>
<ol>
<li>客户端通过原型接口请求克隆方法，传递一个现有对象的实例。</li>
<li>具体原型类实现克隆方法，将自身复制一份，生成一个新的实例。</li>
<li>客户端接收到克隆的对象，得到一个新的实例，而不需要了解对象是如何创建的。</li>
</ol>
<p><strong>优点：</strong></p>
<ol>
<li><strong>减少构造过程：</strong> 避免了直接使用构造函数创建对象，通过复制现有对象可以更加灵活、高效地创建新对象。</li>
<li><strong>简化对象创建：</strong> 对象的创建过程被封装在原型类中，客户端只需要关心克隆方法的调用。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当一个系统需要独立于其组成部分，从而允许对这些部分进行替换时。</li>
<li>当要实例化的类在运行时刻指定时，例如通过动态装载。</li>
<li>当一个类的实例只能有几个不同状态组合中的一种时。</li>
</ol>
</blockquote>
</li>
</ul>
</li>
<li><p>结构型模式</p>
<ul>
<li><p>适配器模式</p>
<blockquote>
<p>适配器模式是一种结构型设计模式，它允许将一个类的接口转换成客户端期望的另一个接口。适配器允许原本由于接口不匹配而不能一起工作的类可以协同工作。</p>
<p><strong>主要角色和结构：</strong></p>
<ol>
<li><strong>目标接口（Target Interface）：</strong> 客户端期望的接口，适配器通过实现这个接口来使得被适配者能够与客户端协同工作。</li>
<li><strong>适配器（Adapter）：</strong> 实现了目标接口，并包含一个对被适配者的引用。适配器通过调用被适配者的接口方法来实现目标接口。</li>
<li><strong>被适配者（Adaptee）：</strong> 需要被适配的类，它的接口与目标接口不匹配。</li>
<li><strong>客户端（Client）：</strong> 通过目标接口与适配器进行交互，而不直接与被适配者交互。</li>
</ol>
<p><strong>工作原理：</strong></p>
<ol>
<li>客户端通过目标接口与适配器交互。</li>
<li>适配器包含一个对被适配者的引用。</li>
<li>当客户端调用目标接口的方法时，适配器通过调用被适配者的接口方法来实现目标接口。</li>
</ol>
<p><strong>优点：</strong></p>
<ol>
<li><strong>解耦性：</strong> 允许客户端与被适配者独立工作，无需知道被适配者的具体实现。</li>
<li><strong>复用性：</strong> 可以复用已有的类，而无需修改其源代码。</li>
<li><strong>灵活性：</strong> 可以动态选择适配器，使得客户端可以适配多个不同的被适配者。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当你希望使用一个已经存在的类，而它的接口不符合你的需求。</li>
<li>当你希望创建一个可复用的类，与一些不相关的类协同工作。</li>
</ol>
</blockquote>
</li>
<li><p>装饰器模式</p>
<blockquote>
<p>装饰器模式是一种结构型设计模式，它允许动态地将责任附加到对象上。装饰器提供了一种灵活的方式，可以在不改变原始对象接口的情况下，对其功能进行扩展或修改。</p>
<p><strong>主要角色和结构：</strong></p>
<ol>
<li><strong>组件（Component）：</strong> 定义一个对象接口，可以动态地给对象添加职责。</li>
<li><strong>具体组件（Concrete Component）：</strong> 实现了组件接口的具体对象，是被装饰的原始对象。</li>
<li><strong>装饰器（Decorator）：</strong> 持有一个指向组件对象的引用，并实现与组件接口一致的接口。可以有多个具体装饰器，它们可以按一定的顺序进行组合。</li>
<li><strong>具体装饰器（Concrete Decorator）：</strong> 实现了装饰器接口的具体对象，包含一个指向被装饰对象的引用。具体装饰器通常会在调用父类的方法前后添加额外的功能。</li>
</ol>
<p><strong>工作原理：</strong></p>
<ol>
<li><strong>客户端通过组件接口与具体组件进行交互。</strong></li>
<li><strong>装饰器类继承自组件接口，持有一个指向具体组件的引用。</strong></li>
<li><strong>具体装饰器通过调用被装饰对象的方法，并在调用前后添加额外的功能。</strong></li>
</ol>
<p><strong>优点：</strong></p>
<ol>
<li><strong>灵活性：</strong> 可以动态地添加或修改对象的行为，而无需修改其源代码。</li>
<li><strong>复用性：</strong> 可以通过组合不同的装饰器实现不同的行为组合，提高代码的复用性。</li>
<li><strong>开闭原则：</strong> 可以在不修改现有代码的情况下扩展对象的行为。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当你希望动态地给对象添加额外的职责，而不影响其接口。</li>
<li>当你希望通过组合不同的装饰器，实现不同的行为组合。</li>
</ol>
</blockquote>
</li>
<li><p>代理模式</p>
<blockquote>
<p>代理模式是一种结构型设计模式，它允许通过代理对象控制对另一个对象的访问。这种类型的设计模式属于结构型模式，因为它通过组合的方式创建了一个包含原始对象的代理对象。</p>
<p><strong>工作原理：</strong></p>
<ol>
<li><strong>客户端通过与代理对象接口交互。</strong></li>
<li><strong>代理对象包含一个指向真实对象的引用。</strong></li>
<li><strong>代理对象在处理客户端请求时，可以在调用真实对象的方法前后添加额外的逻辑。</strong></li>
</ol>
<p><strong>优点：</strong></p>
<ol>
<li><strong>控制访问：</strong> 代理可以控制对真实对象的访问，例如在访问前后执行一些操作。</li>
<li><strong>解耦：</strong> 代理模式可以将客户端与真实对象解耦，客户端只需与代理对象交互即可。</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li><strong>增加复杂性：</strong> 引入了代理对象，可能会增加系统的复杂性。</li>
<li><strong>性能损耗：</strong> 在一些情况下，代理模式可能引入额外的性能开销。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当需要控制对对象的访问，并在访问前后执行一些额外的操作时。</li>
<li>当需要在不改变现有代码的情况下，引入新的功能或修改原有功能时。</li>
</ol>
</blockquote>
</li>
<li><p>外观模式</p>
<blockquote>
<p>外观模式（Facade Pattern）是一种结构型设计模式，它提供了一个简化系统接口的高层接口，使系统更易于使用。外观模式定义了一个高层接口，这个接口使得子系统更容易使用。</p>
<p><strong>主要角色和结构：</strong></p>
<ol>
<li><strong>外观（Facade）：</strong> 提供了一个简化系统操作的高层接口，它包含了对子系统各组件的引用，并负责协调它们的工作。</li>
<li><strong>子系统（Subsystems）：</strong> 实现了系统的各个组件，处理外观对象指派的任务。子系统并不知道外观的存在，对于子系统而言，外观仅仅是另一个客户端而已。</li>
</ol>
<p><strong>工作原理：</strong></p>
<ol>
<li><strong>客户端通过与外观对象交互来完成任务。</strong></li>
<li><strong>外观对象将任务分派给子系统的各个组件，协调它们的工作。</strong></li>
<li><strong>客户端无需直接与子系统的各个组件交互，而是通过外观对象简化了与系统的交互。</strong></li>
</ol>
<p><strong>优点：</strong></p>
<ol>
<li><strong>简化接口：</strong> 外观模式提供了一个简化的高层接口，隐藏了系统的复杂性。</li>
<li><strong>解耦：</strong> 外观模式将客户端与子系统解耦，客户端无需了解子系统的具体实现。</li>
<li><strong>易于使用：</strong> 外观模式使得系统更易于使用，客户端只需与外观对象交互，而无需了解系统内部的复杂逻辑。</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li><strong>违反开闭原则：</strong> 如果需要添加新的功能，可能需要修改外观类。</li>
<li><strong>不符合单一职责原则：</strong> 外观对象通常需要知道子系统的各个组件，可能承担过多的职责。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当需要为复杂的子系统提供一个简化的接口时。</li>
<li>当希望将系统的复杂性与客户端分离，使客户端更容易使用系统时。</li>
<li>当需要划分层次结构时，使用外观模式定义每个层的入口点。</li>
</ol>
</blockquote>
</li>
<li><p>桥接模式</p>
<blockquote>
<p>桥接模式（Bridge Pattern）是一种结构型设计模式，它将一个抽象部分与其实现部分分离，使它们可以独立地变化。这种模式涉及到一个作为桥梁的接口，使得抽象部分和实现部分可以独立地变化而互不影响。</p>
<p><strong>优点：</strong></p>
<ol>
<li><strong>解耦抽象和实现：</strong> 桥接模式将抽象和实现分离，使它们可以独立变化，降低了它们之间的耦合度。</li>
<li><strong>可扩展性：</strong> 可以独立地扩展抽象和实现，不影响彼此。</li>
<li><strong>隐藏实现细节：</strong> 客户端只需关心抽象部分，不需要了解具体的实现细节。</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li><strong>增加复杂性：</strong> 桥接模式增加了系统的复杂性，需要额外定义抽象和实现两个层次。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当系统有多个独立变化的维度，且希望独立管理这些维度时。</li>
<li>当希望在抽象和实现部分之间建立稳定的连接，但又不希望使用继承时。</li>
<li>当希望在多个层次之间建立联系时，例如 GUI 系统中的窗口和不同操作系统的实现。</li>
</ol>
<p><strong>示例：</strong> 考虑一个图形绘制的例子，有不同的图形（圆、矩形）和不同的颜色（红色、蓝色），桥接模式可以将图形和颜色分离，通过组合的方式创建不同的图形。这样，即使增加新的图形或颜色，也不会影响彼此。</p>
</blockquote>
</li>
<li><p>组合模式</p>
<blockquote>
<p>组合模式（Composite Pattern）是一种结构型设计模式，它允许将对象组合成树形结构来表示“部分-整体”的层次结构。通过组合，客户端可以统一对待单个对象和对象的组合。</p>
<p><strong>主要角色和结构：</strong></p>
<ol>
<li><strong>组件（Component）：</strong> 定义组合中所有对象的通用接口，可以是叶子节点或容器节点。</li>
<li><strong>叶子节点（Leaf）：</strong> 表示组合中的叶子节点对象，它没有子节点。</li>
<li><strong>容器节点（Composite）：</strong> 包含叶子节点和&#x2F;或其他容器节点的对象，可以递归地定义子节点。</li>
</ol>
<p><strong>优点：</strong></p>
<ol>
<li><strong>统一接口：</strong> 客户端对待单个对象和组合对象的方式是一致的，不需要区分。</li>
<li><strong>灵活性：</strong> 可以在运行时动态地添加或移除子节点，灵活地组织和管理对象。</li>
<li><strong>可扩展性：</strong> 可以轻松地添加新的叶子节点或容器节点，扩展系统功能。</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li><strong>限制类型：</strong> 定义统一接口可能会限制组件的类型，因为需要满足组件接口的要求。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当希望客户端统一处理单个对象和组合对象时。</li>
<li>当对象的部分-整体层次结构形成树状结构，并且希望客户端能够一致地处理整体和部分时。</li>
<li>当希望动态地组织和管理对象时。</li>
</ol>
<p><strong>示例：</strong> 考虑一个组织结构的例子，其中有部门、员工等不同的对象，可以使用组合模式将它们组织成一个树形结构。这样，可以通过统一的接口处理整个组织，不论是部门还是员工。</p>
</blockquote>
</li>
<li><p>享元模式</p>
<blockquote>
<p>享元模式（Flyweight Pattern）是一种结构型设计模式，旨在通过共享尽可能多的相似对象以减少内存和计算开销。它适用于大量细粒度的对象，通过共享这些对象的公共部分来节省内存。</p>
<p><strong>主要角色和结构：</strong></p>
<ol>
<li><strong>享元接口（Flyweight）：</strong> 定义了具体享元类需要实现的接口，通过这个接口可以接受并作用于外部状态。</li>
<li><strong>具体享元（ConcreteFlyweight）：</strong> 实现了享元接口，包含内部状态和共享的外部状态。具体享元对象需要是可共享的。</li>
<li><strong>不可共享的享元（UnsharedConcreteFlyweight）：</strong> 通常不被共享的具体享元类，包含内部状态但不包含外部状态。</li>
<li><strong>享元工厂（FlyweightFactory）：</strong> 负责创建和管理享元对象，提供对享元对象的访问。在创建享元对象时，如果存在相同的对象，可以返回已存在的对象，否则创建新对象。</li>
</ol>
<p><strong>工作原理：</strong></p>
<ol>
<li><strong>内部状态（Intrinsic State）：</strong> 存储在享元对象内部且不会随外部环境改变的状态。</li>
<li><strong>外部状态（Extrinsic State）：</strong> 存储在享元对象外部且会随外部环境改变的状态。需要在使用时由客户端传递给享元对象。</li>
</ol>
<p><strong>优点：</strong></p>
<ol>
<li><strong>节省内存：</strong> 通过共享相似对象的内部状态，减少了存储大量相似对象所需的内存。</li>
<li><strong>提高性能：</strong> 减少了相似对象的数量，降低了系统的复杂度，提高了性能。</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li><strong>增加复杂性：</strong> 引入了共享机制，可能会使系统变得更加复杂。</li>
<li><strong>外部状态管理：</strong> 外部状态需要在使用时进行传递，可能需要复杂的管理机制。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当一个应用程序使用大量相似对象，占用大量内存时。</li>
<li>对象的大多数状态可以变为外部状态。</li>
<li>对象的外部状态相对独立，而且可以在多个对象间共享。</li>
</ol>
</blockquote>
</li>
</ul>
</li>
<li><p>行为型模式</p>
<ul>
<li><p>策略模式</p>
<blockquote>
<p>策略模式（Strategy Pattern）是一种行为型设计模式，它定义了一系列算法，将每个算法封装并使它们可以互换，使得算法的变化独立于使用算法的客户端。</p>
<p><strong>主要角色和结构：</strong></p>
<ol>
<li><strong>上下文（Context）：</strong> 维护一个对策略对象的引用，并可以切换当前的策略。</li>
<li><strong>策略接口（Strategy）：</strong> 定义了所有支持的算法的公共接口。具体策略类实现了这个接口。</li>
<li><strong>具体策略（Concrete Strategy）：</strong> 实现了策略接口的具体算法。</li>
</ol>
<p><strong>工作原理：</strong></p>
<ol>
<li><strong>定义一组算法：</strong> 将一组算法定义成策略接口。</li>
<li><strong>封装每个算法：</strong> 将每个算法封装成具体策略类。</li>
<li><strong>维护一个上下文：</strong> 上下文类维护一个对策略对象的引用，可以在运行时切换策略。</li>
<li><strong>客户端使用：</strong> 客户端通过上下文使用不同的策略，可以动态选择合适的策略。</li>
</ol>
<p><strong>优点：</strong></p>
<ol>
<li><strong>算法独立性：</strong> 客户端与具体算法解耦，可以独立地改变或替换算法。</li>
<li><strong>复用性：</strong> 可以在不修改现有代码的情况下引入新的算法。</li>
<li><strong>扩展性：</strong> 新的策略可以很容易地添加，不影响已有的策略。</li>
</ol>
<p><strong>缺点：</strong></p>
<ol>
<li><strong>客户端需要了解策略：</strong> 客户端需要了解所有的策略，并选择合适的策略。</li>
</ol>
<p><strong>适用场景：</strong></p>
<ol>
<li>当一个系统有多个变化的算法，并且需要在运行时动态选择其中一个时。</li>
<li>当一个类定义中有多种行为，可以使用策略模式来定义这些行为，使得类的对象在运行时可以选择行为。</li>
<li>当一个系统需要动态地在几种算法中选择一种。</li>
</ol>
</blockquote>
</li>
<li><p>模版方法模式</p>
</li>
<li><p>command模式</p>
<blockquote>
<p>Command 模式是一种行为设计模式，它将一个请求封装成一个对象，从而允许使用不同的请求、队列或日志来参数化客户端对象。它也支持可撤销的操作。</p>
<p><strong>结构：</strong></p>
<ul>
<li><strong>Command（命令）：</strong> 声明了执行操作的接口。</li>
<li><strong>ConcreteCommand（具体命令）：</strong> 实现 Command 接口，具体命令对象，通常会包含接收者对象，并调用接收者的方法来执行具体操作。</li>
<li><strong>Client（客户端）：</strong> 创建命令对象并设置其接收者。</li>
<li><strong>Invoker（调用者）：</strong> 要求命令执行请求。</li>
<li><strong>Receiver（接收者）：</strong> 知道如何实施与执行一个请求相关的操作。</li>
</ul>
<p><strong>工作流程：</strong></p>
<ol>
<li><strong>客户端创建命令对象：</strong> 客户端创建具体的命令对象，并设置该命令对象的接收者。</li>
<li><strong>命令传递给调用者：</strong> 客户端将命令对象传递给调用者。</li>
<li><strong>调用者执行命令：</strong> 调用者执行命令，命令对象调用接收者的方法执行具体操作。</li>
</ol>
<p><strong>优点：</strong></p>
<ul>
<li><strong>解耦发送者和接收者：</strong> 命令模式通过将请求发送者和请求接收者解耦，使得发送者不需要知道接收者的具体实现。</li>
<li><strong>支持撤销和重做：</strong> 可以轻松实现命令的撤销和重做。</li>
</ul>
<p><strong>适用场景：</strong></p>
<ul>
<li>需要将请求发送者和请求接收者解耦的情况。</li>
<li>需要支持撤销和重做操作的情况。</li>
<li>需要在不同时间指定请求、排队请求、或者对请求进行参数化的情况。</li>
</ul>
<p><strong>例子：</strong> 假设我们有一个简单的遥控器（调用者），它有几个按钮，每个按钮对应一个命令。每个命令都与一个特定的电器（接收者）关联，比如电灯、音响等。按下按钮后，命令会执行相应的操作，比如打开电灯、关闭音响。这样，我们就可以通过修改命令对象，而不用修改遥控器来增加或修改功能。这就是 Command 模式的应用。</p>
</blockquote>
</li>
<li><p>中介者模式</p>
<blockquote>
<p>中介者模式（Mediator Pattern）是一种行为设计模式，它定义了一个对象，该对象封装了一组对象之间的交互方式。中介者模式使对象之间的通信变得松散，从而减少对象之间的直接依赖关系。</p>
<p>在中介者模式中，对象不再直接相互通信，而是通过中介者进行通信。这可以减少对象之间的耦合，使系统更容易维护和扩展。中介者模式通常用于处理对象之间复杂的通信和控制流程。</p>
<p>典型的中介者模式包括一个中介者对象，多个同事对象，这些同事对象通过中介者进行通信。当一个同事对象发生变化时，它不直接通知其他同事对象，而是通过中介者进行通知，由中介者来协调处理。</p>
<p>中介者模式的优点包括降低了对象之间的耦合度，使系统更易于维护和扩展。然而，过度使用中介者模式可能导致中介者对象过于庞大复杂，不利于维护。</p>
<p>中介者模式的经典实例是图形用户界面（GUI）框架，其中用户界面组件通过中介者进行交互。</p>
</blockquote>
</li>
<li><p>责任链模式</p>
<blockquote>
<p>责任链模式（Chain of Responsibility Pattern）是一种行为设计模式，它允许你将请求沿着处理者链进行发送，直至有一个处理者处理该请求。这种模式将接收者对象连接成一条链，每个接收者对象都包含对另一个接收者对象的引用。如果一个对象不能处理请求，它会将请求传递给链上的下一个接收者。</p>
<p>主要参与对象包括：</p>
<ol>
<li><strong>处理者接口（Handler）</strong>：定义了处理请求的接口，通常包含一个处理请求的方法（如<code>handleRequest</code>）。</li>
<li><strong>具体处理者类（ConcreteHandler）</strong>：实现了处理者接口，负责处理它所能处理的请求，以及设置下一个处理者的引用。</li>
</ol>
<p>责任链模式的特点包括：</p>
<ul>
<li>请求者并不知道是哪个对象处理了它的请求，处理者的顺序可以动态改变。</li>
<li>可以灵活地添加或修改处理者，不影响已有的处理者和客户端代码。</li>
<li>请求可能因为链上的所有处理者都无法处理而被忽略，也可以被链上的某个处理者成功处理。</li>
</ul>
<p>责任链模式通常应用于以下场景：</p>
<ul>
<li>多个对象可以处理同一请求，但具体哪个对象处理由运行时决定。</li>
<li>在不明确指定接收者的情况下，向多个对象中的一个提交一个请求。</li>
<li>可以动态指定处理请求的对象集合。</li>
</ul>
</blockquote>
</li>
<li><p>状态模式</p>
<blockquote>
</blockquote>
</li>
</ul>
</li>
</ul>
</li>
</ol>
]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
  <entry>
    <title>算法</title>
    <url>/2023/09/18/%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h1 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h1><p><a href="https://leetcode.cn/problems/linked-list-cycle-ii/">1.循环链表出口问题</a></p>
<blockquote>
<p>题目描述：给出一个链表的头节点head，返回链表开始入环的第一个节点，没有则返回nul。</p>
</blockquote>
<p>实例：</p>
<img src="/2023/09/18/%E7%AE%97%E6%B3%95/%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8.png" class="" title="实例">

<blockquote>
<p>输入：head &#x3D; [3,2,0,-4], pos &#x3D; 1<br>输出：返回索引为 1 的链表节点<br>解释：链表中有一个环，其尾部连接到第二个节点。</p>
</blockquote>
<h2 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h2><p>​	我们可以定义两个指针，一个慢指针slow每次走一格，一个快指针fast每次走两格。如果链表存在环路则fast一定会追上slow。如果fast经过一定的循环变为NULL则该链表不存在环路。</p>
<p>​	如果链表中存在环路，那么设从头节点到环路入口的长度为x，设从环路入口到快慢节点相遇的位置长度为y，环中剩下的长度为z。<br>$$<br>slow指针走的距离&#x3D;x+y<br>$$</p>
<p>$$<br>fast指针走的距离&#x3D;x+y+n(y+z)<br>$$</p>
<p>​	又因为fast移动的速度是slow的两倍<br>$$<br>(x+y)*2&#x3D;x+y+n(y+z)<br>$$</p>
<p>$$<br>x&#x3D;n(y+z)-y<br>$$</p>
<p>$$<br>x&#x3D;(n-1)(y+z)+z<br>$$</p>
<p>​	所以可以看出来，在进入环之前的长度，是N-1倍的环长加上相遇点到入口点的距离之外的距离。而且n必定大于等于1。因为快慢节点相遇之前，快节点肯定要先在环中跑一圈才能遇到slow。</p>
<p>​	如果此时我们设置两个指针分别从head和入口点每次跳动一格的距离，那么下次相遇的时候必定是在入口点。下面给出具体的代码。</p>
<pre class="line-numbers language-c++" data-language="c++"><code class="language-c++">if(!head) return head; &#x2F;&#x2F;如果头节点为空，直接返回
ListNode* fast &#x3D; head; &#x2F;&#x2F;让快慢节点从头开始
ListNode* slow &#x3D; head;
while(fast&amp;&amp;slow)&#123;
  slow &#x3D; slow-&gt;next;
  if(fast-&gt;next) fast &#x3D; fast-&gt;next-&gt;next;
  if(fast&#x3D;&#x3D;slow) break;
&#125;

if(!fast) return null; &#x2F;&#x2F;如果快指针为空，则证明该链表没有环路

ListNode*p &#x3D; head;
ListNode*q &#x3D; fast;
while(p!&#x3D;q)&#123;
  p&#x3D;p-&gt;next;
  q&#x3D;q-&gt;next;
&#125;
return p;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>



<h1 id="字符串匹配"><a href="#字符串匹配" class="headerlink" title="字符串匹配"></a>字符串匹配</h1>]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
  <entry>
    <title>科软高级软件工程课堂问答</title>
    <url>/2023/10/24/%E7%A7%91%E8%BD%AF%E9%AB%98%E7%BA%A7%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B%E8%AF%BE%E5%A0%82%E9%97%AE%E7%AD%94/</url>
    <content><![CDATA[<ol>
<li>软件危机解决了吗？为什么？</li>
</ol>
<blockquote>
<ol>
<li>首先我们要理解软件危机产生的原因是由于”无法满足社会发展的需要”导致的</li>
<li>软件危机包含了两方面的问题：一、如何开发软件，以满足不断增长，日趋复杂的需求。二、如何维护数量不断膨胀的软件产品。</li>
<li>综合上面两点原因，我认为软件危机并没有解决。因为我们的社会时时刻刻在发生着变化，这就需要我们的软件要不断的就行更新迭代，同时还要不断的处理软件运行时出现的各种各样的BUG。所以软件危机并没有真正的解决。</li>
</ol>
</blockquote>
<ol start="2">
<li>工程化软件开发与编程之间可以直接画等号吗？不编程也可以实现软件开发？（例如我是项目经理）</li>
</ol>
<blockquote>
<ol>
<li>不可以直接画等号，因为工程化软件开发是开发出高质量的软件产品，满足用户需求，提高软件开发效率和可维护性。而编程仅仅指的是代码编写的阶段（即coding的过程）。</li>
<li>不编程也可以实现软件的开发。在软件开发中，编程只是其中的一个环节。对于项目经理来说只需要带领团队做好需求分析、搭建好项目的整体框架等内容即可，不需要进行编程。</li>
</ol>
</blockquote>
<ol start="3">
<li>改变软件工程的七个关键因素，那七个？每个因素带来了哪些改变？以及新的因素有哪些？并带来了哪些影响</li>
</ol>
<blockquote>
<p>Wasserman 于1996年提出了七个该改变软件工程的关键因素：</p>
<ol>
<li>交付时间的重要性（现在的软件交付时间是以小时来进行衡量的，而原来的软件交付时间是以月甚至年来进行衡量）</li>
<li>计算行业经济的变化趋势（目前计算机行业是蓬勃发展的时代，有着大量的资金支持。相较于之前，计算机主要运用于军工领域，缺乏活力和资金支持 ）</li>
<li>功能强大的桌面运算（目前的主流的系统为了方便大众的应用，都配备了优美且边界的图形界面。但是原来的计算机都是通过命令行来进行操作的，只有专业的计算机领域人员和数学相关人员来可以进行操作）</li>
<li>互联网络（目前全世界的海量的PC都通过互联网连接在一起，每个人可以时时的分享自己的数据。但是之前的计算主要应用于军工，互联性不是很高）</li>
<li>面向对象的技术（随着时间的推移，面向对象的技术出现，更加有利于目前的欢迎。但是原来的软件工程更加的注重过程）</li>
<li>图形用户界面（我觉得这个和第三点很相似）</li>
<li>瀑布模型的不可预知性（因为瀑布模型需要先进行分析，然后在根据分析出的需求。但是当中途用户需要更改需求时，瀑布模型就会不知道该如何做）</li>
</ol>
</blockquote>
<blockquote>
<p>除了wasserman提出的因素外我还想到了一下两点因素：</p>
<ol>
<li>新技术和工具：不断涌现的新技术、开发工具和框架可以改变软件工程实践。例如，云计算、大数据、人工智能和区块链等技术的发展对软件开发产生了深远影响，鼓励开发人员采用新方法和工具。</li>
<li>开发方法论：新的开发方法论和方法，如敏捷开发、DevOps、微服务架构等，已经改变了软件开发的方式。这些方法鼓励更快的迭代、更紧密的合作和更高效的交付。</li>
</ol>
</blockquote>
<ol start="4">
<li><p>超越的观点和用户的观点都是不可取的，要使用制造的观点和产品的观点。用户是在末端进行评价，这时已经不能够再改变产品。</p>
<blockquote>
<ol>
<li>超越的观点：质量是可以背认识而不能被定义的。（如果质量只能够被认识那么就不会有好坏之分，又何谈软件是否是一个好软件呢？）</li>
<li>用户的观点：因为用户是在开发完成之后才能进行质量检测，但是此时已经开发完毕，不能够对产品再次进行修改</li>
<li>制造的观点：质量是与需求说明的一致</li>
<li>产品的观点：质量是鱼产品的特性相联系的</li>
<li>基于价值的观点：质量决定于顾客愿意支付的金额</li>
</ol>
</blockquote>
</li>
<li><p>系统的分析方法有哪些步骤？<img src="/2023/10/24/%E7%A7%91%E8%BD%AF%E9%AB%98%E7%BA%A7%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B%E8%AF%BE%E5%A0%82%E9%97%AE%E7%AD%94/%E6%88%AA%E5%B1%8F2023-10-24%2016.45.13-8164097.png" class=""></p>
</li>
<li><p>软工与计科的区别的是什么？</p>
<blockquote>
<p>计算机科学：研究计算机相关的理论</p>
<p>软件工程：把计算机技术看成工具，用以解决问题</p>
<p>就像老师上课讲的工程学位和工学学位的区别一样，一个是进行理论研究，产出新的idea、新的方法，而软工是去进行这些内容的实现和使用。一个偏理论，一个偏实践</p>
</blockquote>
</li>
<li><p>软件开发的三个阶段：传统、敏捷开发、开发运维一体化（自己对应ppt去看）</p>
<blockquote>
<ol>
<li>上世纪90年代，“软件过程也是软件”以及开发优秀软件过程的重要性的到广泛认同（卡耐基梅隆大学先后提出了软件开发能力成熟度模型CMM，用户评估和改进软件过程，后来发展为CMMI）</li>
<li>进入本世纪00年代，敏捷开发的思想成为主流（人们更加关注快速的开发和交付书对，而传统的软件过程过度强调计划，很难适应目前的需求）</li>
<li>进入本世纪10年代后，开发韵味一体化成为主流（大量软件开发需求朝着快速、易变的方向发展，敏捷的思想和实践进入运维领域。打破了开发和运维的界限，实现了从开发到运维的快速反应）</li>
</ol>
</blockquote>
</li>
<li><p>关键路径是不是活动的最优选择？</p>
<blockquote>
<p>首先我们要理解什么是关键路径：</p>
<p>关键路径是指在关键路径上的活动是不可被忽视的活动，他们对于整个项目的工期有着直接的影响。但是关键路径上的活动并不一定就是最终要的活动，也不一定是最优的选择。</p>
</blockquote>
</li>
<li><p>用自己的理解说一下软件生命周期和软件过程的理解（对软件过程可以从个体、整体和工程的视角的进行观察，例如：程序员、项目经理和架构师）</p>
<blockquote>
<ol>
<li>软件生命周期：指软件从开始到结束的整个过程，包括规划、开发、测试、部署和维护等阶段，描述了软件的整体状态和变化。</li>
<li>软件过程：软件过程是指软件开发中所执行的一系列活动、动作和任务的集合。</li>
</ol>
<img src="/2023/10/24/%E7%A7%91%E8%BD%AF%E9%AB%98%E7%BA%A7%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B%E8%AF%BE%E5%A0%82%E9%97%AE%E7%AD%94/%E6%88%AA%E5%B1%8F2023-10-25%2011.35.00-8205039.png" class="">

<p>我们可以从上面这三个层次来理解软件过程</p>
</blockquote>
</li>
<li><p>要知道传统开发模式的三个时期和八个阶段是什么？</p>
<blockquote>
<img src="/2023/10/24/%E7%A7%91%E8%BD%AF%E9%AB%98%E7%BA%A7%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B%E8%AF%BE%E5%A0%82%E9%97%AE%E7%AD%94/%E6%88%AA%E5%B1%8F2023-10-25%2011.39.14-8205177.png" class="">
</blockquote>
</li>
<li><p>瀑布模型的优缺点？（最好看看课本和课件上各种模型的优缺点）</p>
<blockquote>
<p>优点：</p>
<ol>
<li>软件生命周期模型，使软件开发过程可以在分析、设计、编码、测试和维护的框架下进行。</li>
<li>软件开发过程具有系统性、可控性，克服了软件开发的随意性。</li>
</ol>
<p>缺点：</p>
<ol>
<li>项目开始阶段用户很难精确的提出产品需求，随着客户对系统的深入理解，修改需求会十分常见，但是瀑布模型对于需求的修改会推到之前的所有工资重新开始，会导致耗费大量的人力物力做无用功。</li>
<li>项目开发晚期才能得到程序的运行版本，这个时候软件需求和开发中的错误会造成巨大的代价。</li>
<li>采用线性模型组织项目开发经常发生开发小组人员“堵塞状态”，特别是项目的开始和结束的阶段。</li>
</ol>
</blockquote>
</li>
<li><p>人的认知是螺旋上升的，所以就可以看出演化模型螺旋形状的合理性。</p>
</li>
<li><p>传统强调物性、现代强调人性</p>
</li>
<li><p>RUP的定义：用例驱动、以体系结构为核心、迭代及增量的软件工程框架，由UML方法和工具支持。</p>
</li>
<li><p>请描述一下RUP的可伸缩性，举个例子说明？（可伸缩就是在瀑布和敏捷之间的调节）</p>
</li>
<li><p>RUP是否是每个阶段都是全过程的，构建阶段是否是只需要写代码？如果不是，那请问还要做什么？如果也需要做需求，那需要做什么样的需求？</p>
</li>
<li><p>敏捷的核心思想：可执行的最大价值和人</p>
</li>
<li><p>敏捷开发的四个原则：</p>
</li>
</ol>
<ul>
<li>人大于物</li>
<li>软件大于文档</li>
<li>合作大于谈判</li>
<li>人的认知时间在两周之内，所以相应变化也需要在两周之内（即敏捷的增量迭代需要短时间内的开发，这个就是相应变化胜过遵循计划）</li>
</ul>
<ol start="19">
<li><p>敏捷的12条原则</p>
<blockquote>
<ol>
<li>我们的最高优先级是持续不断地、及早地交付</li>
</ol>
<p>有价值的软件来使客户满意</p>
<ol start="2">
<li>拥抱变化，即使是在项目开发的后期，愿意为</li>
</ol>
<p>了客户的竞争优势而采取变化</p>
<ol start="3">
<li>经常地交付可工作的软件，相隔几星期或一两</li>
</ol>
<p>个月，倾向于采取较短的周期</p>
<ol start="4">
<li><p>业务人员和开发人员在整个过程中紧密合作</p>
</li>
<li><p>围绕着被激励的个体构建项目，为个体提供所</p>
</li>
</ol>
<p>需的环境和支持，给予信任，从而达成目标</p>
<ol start="6">
<li>在团队内和团队间沟通信息的最有效和最高效</li>
</ol>
<p>的方式是面对面的交流</p>
<ol start="7">
<li><p>可工作的软件是衡量进度的首要标准</p>
</li>
<li><p>倡导可持续开发，项目发起者、开发人员和</p>
</li>
</ol>
<p>用户应该维持一个可持续的步调</p>
<ol start="9">
<li>持续地追求技术卓越和良好设计，可以提高</li>
</ol>
<p>敏捷性</p>
<ol start="10">
<li><p>以简洁为本，它是减少不必要工作的艺术</p>
</li>
<li><p>最好的体系结构、需求和设计是从自组织的</p>
</li>
</ol>
<p>团队中涌现出来的</p>
<ol start="12">
<li>团队定期地反思如何变得更加高效，并相应</li>
</ol>
<p>地调整自身的行为</p>
</blockquote>
</li>
<li><p>测试驱动开发的优点：</p>
</li>
</ol>
<ul>
<li>操作正确性，按照用户story，符合story的需求</li>
<li>不同的观察点</li>
<li>解除软件的耦合性</li>
<li>有利于改变软件的体系结构</li>
</ul>
<ol start="21">
<li>怎么理解这个内容？（我们在进行极限编程时只需要使用其中的一点就是属于极限编程）<img src="/2023/10/24/%E7%A7%91%E8%BD%AF%E9%AB%98%E7%BA%A7%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B%E8%AF%BE%E5%A0%82%E9%97%AE%E7%AD%94/%E6%88%AA%E5%B1%8F2023-10-24%2017.07.12-8164113.png" class=""></li>
<li>表面的需求和真实的需求？（挖掘真正的需求、细化需求）</li>
<li>什么是需求和需求工程？（需求工程：课件上有）</li>
<li>银弹问题（即没有解决问题一劳永逸的方法：No sliver Bullet）</li>
<li>工程实践中会用到7中方法中的哪些方法？</li>
<li>请结合自己的工程实践说明一下需求导出的过程？</li>
<li>对三个文档的理解：（初步“产品要求”文档、产品需求说明文档、SRS）</li>
<li>结合抽象层次谈一下多态（老师使用的神行走于人世间，可以编程男人和女人）</li>
<li>面向对象开发的过程模型和结构化开发的过程模型，从产出产品的角度来说明？</li>
<li>过程论和对象论的区别？</li>
</ol>
]]></content>
      <tags>
        <tag>posts</tag>
      </tags>
  </entry>
</search>
